{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Guardium Data Protection Bootcamp Labs","text":"<p>Welcome to the IBM Security Guardium Data Protection (GDP) Bootcamp laboratory documentation. This portal provides comprehensive, step-by-step guides for all hands-on exercises designed for the IBM Technology Zone (TechZone) training environment.</p>"},{"location":"#about-the-lab-environment-automation","title":"About the Lab Environment &amp; Automation","text":"<p>These lab exercises reflect the exact procedures automated in the companion <code>guardium_tz_bootcamp_automation</code> project. The automation framework allows instructors and participants to provision, configure, and validate stages of the bootcamp automatically, while this documentation serves as the reference walkthrough for manual execution and deep-dive learning.</p>"},{"location":"#lab-curriculum-overview","title":"Lab Curriculum Overview","text":"<p>The bootcamp hands-on program covers core operational, configuration, and security capabilities of Guardium Data Protection:</p> <ol> <li>Environment Access &amp; Connectivity (Lab 00)</li> <li>Gathering deployment details from IBM Technology Zone.</li> <li>Establishing secure SSH and RDP management tunnels via the raptor bastion host.</li> <li> <p>Accessing Central Manager and Collector web user interfaces.</p> </li> <li> <p>Appliances Setup &amp; Central Management (Lab 01)</p> </li> <li>Initial appliance registration to Central Manager (CM).</li> <li>Patch distribution, system verification, and health checks.</li> <li> <p>System backup and configuration archive setup.</p> </li> <li> <p>GIM Management</p> </li> <li>Guardium Installation Manager (GIM) client discovery, setup, and configuration.</li> </ol>"},{"location":"#getting-started","title":"Getting Started","text":"<p>To begin, proceed to the Access Environment (Lab 00) guide to verify connectivity to your provisioned TechZone environment and familiarize yourself with the command syntax notation.</p>"},{"location":"v14/access/","title":"Access environment (Lab 00)","text":""},{"location":"v14/access/#syntax-notation-in-labs","title":"Syntax notation in Labs","text":"<p>Throughout the lab instructions, the following conventions are used:</p> <ul> <li>Command and Option Names: Command names, flags, parameter names, and UI elements appear in bold (e.g., ssh, systemctl, -p).</li> <li>Variable Parameters: Values that you must replace with specific data from your environment are enclosed in angle brackets: <code>&lt;parameter&gt;</code> (e.g., <code>&lt;raptor_public_ip&gt;</code>).</li> <li>Mutually Exclusive Values / Choices: When a parameter or target accepts one of several possible values from a predefined list, options are separated by a vertical bar: <code>|</code> (e.g., <code>cm|coll1|kafka1|appnode1|appnode2</code>).</li> <li>Value Ranges: When a parameter accepts a value within a defined numeric range, the allowed interval is shown in square brackets: <code>[minimum-maximum]</code> (e.g., <code>[1-10]</code>, <code>[0-100]</code>, <code>[1-65535]</code>).</li> </ul>"},{"location":"v14/access/#get-deployed-environment-details","title":"Get deployed environment details","text":"<ol> <li>Select My Requests in IBM Technology Zone to review the environments that are currently available or being deployed. Open the details (Open This Reservation) for the environment you want to access. </li> <li>Confirm that the environment was created from the bootcamp manifest. The environment name should follow the format gdp_bootcamp_v13. The version number is incremented for newer releases of the training environment. Expand the deployment details by clicking the arrow next to the manifest name. </li> <li>The expanded Output list contains key information about machines in the environment. The number of systems may vary depending on the bootcamp version, but the general principle remains the same. Machine names include a random suffix, which can be ignored throughout the lab environment. Some machines and services are accessible directly from the Internet. In those cases, use the public IP addresses listed in the machine descriptions. Record the public IP addresses of the raptor, cm, coll1, and ceratops machines, as you will access these systems directly from your workstation throughout the bootcamp. </li> <li>The cm, coll1, appnode1, appnode2, and kafka1 systems are Guardium appliances. Administrative access to these appliances is performed by connecting to them via SSH from the raptor machine. The appliances use the standard SSH port 22. In addition, the cm and coll1 systems provide access to the Guardium UI, which is available over HTTPS on port 8443. You will access this interface using your local web browser. The sauropod machine is accessible from raptor through SSH on port 2223. </li> </ol>"},{"location":"v14/access/#access-to-machines","title":"Access to machines","text":"<ol> <li>To connect to raptor, use an SSH client on your workstation. On Windows, MobaXterm or the standard PuTTY client are recommended. On macOS, use iTerm or connect directly from the command line using the built-in SSH client. The default password for the environment services is provided in the TechZone environment description. Use this password to sign in to the root account. raptor can be accessed using non-standard SSH port number 2223.  <pre><code>ssh -lroot -p2223 &lt;raptor_public_ip&gt;\n</code></pre></li> <li>To sign in to the cli account on a Guardium appliance, connect via SSH from your raptor session. The password for the cli account is different from the password used for the other environment services and can also be found in the TechZone environment description. <pre><code>ssh -lcli -p22 cm|coll1|kafka1|appnode1|appnode2\n</code></pre> </li> <li>To log in to the sauropod machine, you do not need to provide credentials, as the root accounts on raptor and sauropod are configured to use SSH key-based authentication. <pre><code>ssh sauropod\n</code></pre> </li> <li>For security reasons, RDP access to the ceratops (Windows) machine is not exposed to the Internet. To temporarily access this machine, enable a tunnel on raptor using the following command. <pre><code>systemctl start socat-RDP\n</code></pre></li> <li>To connect to the ceratops Windows machine, use an RDP client on your workstation and specify the raptor public IP address on port 8443. This will forward the connection to the ceratops machine. Sign in using the demo user account and the default environment services password. </li> <li>Ensure that you sign in as .\\demo using the default environment services password. </li> <li>Close RDP session and stop tunnel. You will start it if needed. <pre><code>systemctl stop socat-RDP\n</code></pre></li> <li>For macOS, the recommended way to connect to the ceratops machine is to use the Windows App available from the App Store. Add a new machine by creating a new Configure PC, then enter the public IP address of the raptor machine and port 8443. Sign in using the demo user account and the default environment services password.  </li> <li>The Central Manager and collector GUIs are accessible from a web browser on your workstation. Connect using HTTPS to the appliance's public IP address on port 8443. Do not log into UI yet, only check connectivity and save access page in the Bookmarks to simplify connection later. <pre><code>https://&lt;public_ip_cm|public_ip_coll1&gt;:8443\n</code></pre> </li> </ol>"},{"location":"v14/access/#appendix","title":"Appendix","text":"<p>Dependencies:</p> <p>Resources:</p> <p>Instructor notes:</p> <p>In case of on-site training passwords are provided by instructor In case of TZ automation, check the password file located there</p>"},{"location":"v14/appliance/","title":"Appliance setup","text":""},{"location":"v14/appliance/#appliance-basic-configuration","title":"Appliance basic configuration","text":"<ol> <li>Login to raptor and check local ip address of cm <pre><code>cat /etc/hosts\n</code></pre> </li> <li>Login to cm cli using ssh session on raptor <pre><code>ssh -lcli -p22 cm\n</code></pre></li> <li>Run two commands to pause the data aggregation process. In the test environment, enabled aggregation services may cause unintended delays while waiting for several steps to be completed (the second command will require confirmation). <pre><code>store run_cleanup_orphans_daily off\n\nstore purge_age_period 0\n</code></pre> </li> <li>Display network interfaces and routes to verify connectivity. <pre><code>show network interface all\n\nshow network route default\n</code></pre> </li> <li>Notice that the IP address is set to localhost or temporary deployment IP, which is specific to the IBM Cloud environment. Update the IP address to the correct value based on IP address identified in step 1. <pre><code>store network interface ip &lt;cm_ip_address&gt;/24\n</code></pre> </li> <li>Check DNS resolver configuration. The list will be empty because the appliance is deployed in the cloud. <pre><code>show network resolvers\n</code></pre></li> <li>In this case, you can retrieve the dynamically assigned list of local servers using the following command. <pre><code>show network resolvers_cloud\n</code></pre> </li> <li>Test external DNS resolution by pinging a public hostname. Do not interrupt the test with Ctrl+C, because this may terminate the ssh connection. <pre><code>ping www.ibm.com\n</code></pre></li> <li>Display the current system time settings. <pre><code>show system clock all\n</code></pre> </li> <li>List available time zones.  <pre><code>store system clock timezone list\n</code></pre></li> <li>Set the system time zone to lab timezone. Wait a few minutes until the change is fully applied. All machines in the lab will use GMT (Europe/London) time zone configuration. <pre><code>store system clock timezone Europe/London\n</code></pre> </li> <li>Re-login to cli session on cm and confirm new time zone. <pre><code>show system clock all\n</code></pre> </li> <li>Configure NTP servers (three pool.ntp.org servers) and enable the NTP client. <pre><code>store system time_server hostname 0.pool.ntp.org 1.pool.ntp.org 2.pool.ntp.org\n</code></pre></li> <li>Confirm that they are correctly set. <pre><code>show system time_server all\n</code></pre> </li> <li>Activate NTP client, execute: <pre><code>store system time_server state on\n</code></pre></li> <li>Change the central manager host name to cm. When asked about cloning, answer \u201cy\u201d. This command can take timeout (10 minutes). In that case re-login and confirm than name was set to cm.yourcompany.com and continue. <pre><code>store system hostname cm\n</code></pre> </li> <li>Set the domain name <pre><code>store system domain demo.guardium\n</code></pre> </li> <li>Re-login and confirm that machine prompt is now set to cm.demo.guardium. </li> <li>Restart network, insert Yes to confirm. It is crucial after machine name change because it intiate the GUI certificate recreation. <pre><code>restart network\n</code></pre> </li> <li>Because we have multiple cloned appliances in our environment and all share the same Guardium ID (GID), assign a unique ID to each of them before configuring them to work together, use the following command. <pre><code>store product gid 1111\n</code></pre></li> <li>All appliances in a centrally managed environment must share the same shared secret. Set it to \"guardium\" on cm. <pre><code>store system shared secret guardium\n</code></pre></li> <li>Set cm configuration to accept small disk. <pre><code>store system small_disk\n</code></pre> </li> <li>Increase GUI and CLI session timeouts to avoid unexpected logouts. <pre><code>store gui session_timeout 9999\n\nstore timeout cli_session 600\n</code></pre></li> <li>Display the static host mappings; initially this list is empty. <pre><code>support show hosts\n</code></pre></li> <li>Add static host entries for all lab machines (coll1, appnode1, appnode2, kafka1, raptor, sauropod, ceratops) <pre><code>support store hosts &lt;coll1_ip_address&gt; coll1.demo.guardium\n\nsupport store hosts &lt;appnode1_ip_address&gt; appnode1.demo.guardium\n\nsupport store hosts &lt;appnode2_ip_address&gt; appnode2.demo.guardium\n\nsupport store hosts &lt;kafka1_ip_address&gt; kafka1.demo.guardium\n\nsupport store hosts &lt;raptor_ip_address&gt; raptor.demo.guardium\n\nsupport store hosts &lt;sauropod_ip_address&gt; sauropod.demo.guardium\n\nsupport store hosts &lt;ceratops_ip_address&gt; ceratops.demo.guardium\n</code></pre> </li> <li>Display the host list again and confirm that all entries are correctly configured. <pre><code>support show hosts\n</code></pre> </li> <li>Restart the cm. If the appliance reports that MySQL is archiving, choose \u201cNo\u201d to abort the restart and try again later, as shown in the screenshot. <pre><code>restart system\n</code></pre> </li> <li>Reconnect to the cm and check appliance unit type. The appliance is an aggregator and is not yet configured as a central manager. <pre><code>show unit type\n</code></pre> </li> <li>The system has valid license keys loaded, so you can now promote the aggregator to be a Central Manager. This command may take some time as it activates all services required for managing the appliance group <pre><code>store unit type manager\n</code></pre> </li> <li>You can proceed with the next part of the lab and return to this CLI session once the command above is completed. Then verify that the aggregator has been successfully promoted to a manager. <pre><code>show unit type\n</code></pre> </li> </ol>"},{"location":"v14/appliance/#other-appliances-setup","title":"Other appliances setup","text":"<ol> <li>Sign in to the coll1 collector from raptor and execute a set of configuration commands to prepare the machine for connection to the cm. <pre><code>store run_cleanup_orphans_daily off\n\nstore network interface ip &lt;coll1_ip_address&gt;/24\n</code></pre></li> <li>Set the correct time zone (it should match the one configured on the cm) and re-login to the collector. <pre><code>store system clock timezone Europe/London\n</code></pre></li> <li>Then configure NTP, as well as the host name and domain. Make sure to confirm that the appliance is cloned when prompted during hostname configuration. <pre><code>store system time_server hostname 0.pool.ntp.org 1.pool.ntp.org 2.pool.ntp.org\n\nstore system time_server state on\n\nstore system hostname coll1\n\nstore system domain demo.guardium\n</code></pre></li> <li>Re-login and confirm that the command prompt reflects the configured hostname. </li> <li>Execute the remaining configuration commands and reboot the appliance. <pre><code>restart network\n\nstore product gid 2222\n\nstore system shared secret guardium\n\nstore system small_disk\n\nstore gui session_timeout 9999\n\nstore timeout cli_session 600\n\nsupport store hosts &lt;cm_ip_address&gt; cm.demo.guardium\n\nsupport store hosts &lt;appnode1_ip_address&gt; appnode1.demo.guardium\n\nsupport store hosts &lt;appnode2_ip_address&gt; appnode2.demo.guardium\n\nsupport store hosts &lt;kafka1_ip_address&gt; kafka1.demo.guardium\n\nsupport store hosts &lt;raptor_ip_address&gt; raptor.demo.guardium\n\nsupport store hosts &lt;sauropod_ip_address&gt; sauropod.demo.guardium\n\nsupport store hosts &lt;ceratops_ip_address&gt; ceratops.demo.guardium\n\nsupport show hosts\n\nrestart system\n</code></pre> </li> <li>Now perform the same steps on the appnode1 node. <pre><code>store run_cleanup_orphans_daily off\n\nstore network interface ip &lt;appnode1_ip_address&gt;/24\n\nstore system clock timezone Europe/London\n</code></pre></li> <li>Re-login and continue on the appnode1 <pre><code>store system time_server hostname 0.pool.ntp.org 1.pool.ntp.org 2.pool.ntp.org\n\nstore system time_server state on\n\nstore system hostname appnode1\n\nstore system domain demo.guardium\n</code></pre></li> <li>Re-login to appnode1 and confirm that name is changed <pre><code>restart network\n\nstore product gid 3333\n\nstore system shared secret guardium\n\nstore system small_disk\n\nstore gui session_timeout 9999\n\nstore timeout cli_session 600\n\nsupport store hosts &lt;cm_ip_address&gt; cm.demo.guardium\n\nsupport store hosts &lt;coll1_ip_address&gt; coll1.demo.guardium\n\nsupport store hosts &lt;appnode2_ip_address&gt; appnode2.demo.guardium\n\nsupport store hosts &lt;kafka1_ip_address&gt; kafka1.demo.guardium\n\nsupport store hosts &lt;raptor_ip_address&gt; raptor.demo.guardium\n\nsupport store hosts &lt;sauropod_ip_address&gt; sauropod.demo.guardium\n\nsupport store hosts &lt;ceratops_ip_address&gt; ceratops.demo.guardium\n\nrestart system\n</code></pre></li> <li>Then do the same steps on the appnode2 node. <pre><code>store run_cleanup_orphans_daily off\n\nstore network interface ip &lt;appnode2_ip_address&gt;/24\n\nstore system clock timezone Europe/London\n</code></pre></li> <li>Re-login and continue on the appnode2 <pre><code>store system time_server hostname 0.pool.ntp.org 1.pool.ntp.org 2.pool.ntp.org\n\nstore system time_server state on\n\nstore system hostname appnode2\n\nstore system domain demo.guardium\n</code></pre></li> <li>Re-login to appnode2 and confirm that name is changed <pre><code>restart network\n\nstore product gid 4444\n\nstore system shared secret guardium\n\nstore system small_disk\n\nstore gui session_timeout 9999\n\nstore timeout cli_session 600\n\nsupport store hosts &lt;cm_ip_address&gt; cm.demo.guardium\n\nsupport store hosts &lt;coll1_ip_address&gt; coll1.demo.guardium\n\nsupport store hosts &lt;appnode1_ip_address&gt; appnode1.demo.guardium\n\nsupport store hosts &lt;kafka1_ip_address&gt; kafka1.demo.guardium\n\nsupport store hosts &lt;raptor_ip_address&gt; raptor.demo.guardium\n\nsupport store hosts &lt;sauropod_ip_address&gt; sauropod.demo.guardium\n\nsupport store hosts &lt;ceratops_ip_address&gt; ceratops.demo.guardium\n\nrestart system\n</code></pre></li> <li>We must do this same for kafka1 appliance as well <pre><code>store run_cleanup_orphans_daily off\n\nstore network interface ip &lt;kafka1_ip_address&gt;/24\n\nstore system clock timezone Europe/London\n</code></pre></li> <li>Re-login and continue on kafka1 <pre><code>store system time_server hostname 0.pool.ntp.org 1.pool.ntp.org 2.pool.ntp.org\n\nstore system time_server state on\n\nstore system hostname kafka1\n\nstore system domain demo.guardium\n</code></pre></li> <li>Re-login to kafka1 and confirm that name is changed <pre><code>restart network\n\nstore product gid 5555\n\nstore system shared secret guardium\n\nstore system small_disk\n\nstore gui session_timeout 9999\n\nstore timeout cli_session 600\n\nsupport store hosts &lt;cm_ip_address&gt; cm.demo.guardium\n\nsupport store hosts &lt;coll1_ip_address&gt; coll1.demo.guardium\n\nsupport store hosts &lt;appnode1_ip_address&gt; appnode1.demo.guardium\n\nsupport store hosts &lt;appnode2_ip_address&gt; appnode2.demo.guardium\n\nsupport store hosts &lt;raptor_ip_address&gt; raptor.demo.guardium\n\nsupport store hosts &lt;sauropod_ip_address&gt; sauropod.demo.guardium\n\nsupport store hosts &lt;ceratops_ip_address&gt; ceratops.demo.guardium\n\nrestart system\n</code></pre></li> </ol>"},{"location":"v14/appliance/#add-user-and-create-dashboard","title":"Add user and create dashboard","text":"<ol> <li>Login to cm UI as an accessmgr user (use this same password like you used for cli access to appliances) and press Add User button in Access manager view </li> <li>Create a new user - demo. Set password and description. Expand the Roles panel and select: admin, cli, fam, user and vulnerability assess roles. </li> <li>Scroll slightly down, select Password never expires, then click Add to create the user. </li> <li>The newly created user should appear at the bottom of the list. Then, use the State toggle for the guardium account to deactivate it. </li> <li>Sign out of the accessmgr account and sign in as the newly created demo user. </li> <li>In the Search field, enter License to filter the options. Select the License view and review the installed license keys. The Central Manager, as mentioned earlier, has pre-installed license keys for a centrally managed environment. Functional (append) keys enable DAM, FAM and VA features. </li> <li>Navigate to the Feature Unlock tab and verify that the additional features have been loaded into the system. Note that this does not mean they have been activated. </li> <li>Next, select Definition Export/Import, go to the Import tab, choose the report definition file <code>exp_report_two_of_training_dasboard.sql</code> from your exports folder (student materials), and click Upload file. </li> <li>After a moment, the Full SQL (Training) report should appear in the Uploaded files section. Select it and click Import. </li> <li>After a moment, a confirmation message should appear indicating the report was successfully imported. </li> <li>Repeat the import process, but this time use the <code>exp_default_policy.sql</code> file. This will load the policy named Default bootcamp policy. </li> <li>To create a dashboard, select the My Dashboards icon from the vertical menu, then choose Create New Dashboard. Edit the name (pencil icon) to Training and click Save. Next, click Add Report to open the report selection window and add three reports: the newly imported Full SQL (Training) and the built-in SQL Errors and S-TAP and External S-TAP Statistics. Reports are added to the dashboard upon selection from the list. </li> </ol> <p>Uwaga</p> <p>Use demo account for all UI activities in the bootcamp labs unless a different one is explicitly required.</p>"},{"location":"v14/appliance/#register-appliances","title":"Register appliances","text":"<ol> <li>From cm UI open Central Management application and select Register unit button from Actions list. Notice that there are no appliances managed by our Central Manager yet. Register unit pop-up will provide the possibility to insert coll1 Unit IP (select appropriate one) and default communication port (8443) then press Register button. </li> <li>After a longer wait (be patient), the view should refresh and the coll1 collector will appear in the managed appliances list. Its presence on the list does not yet mean full synchronization\u2014this process may take a few more minutes. </li> <li>Sign in to the coll1 via CLI and notice that the appliance mode has changed from Standalone to Managed. <pre><code>show unit type\n</code></pre> </li> <li>Repeat the full appliance registration process on appnode1, appnode2 and kafka1 and confirm that all appliances are visible from the cm Central management view. </li> </ol>"},{"location":"v14/appliance/#central-manager-patching","title":"Central Manager patching","text":"<p>Screenshots</p> <p>Screenshots in this section may not exactly match the patch numbers available in your environment. The labs are frequently updated, so there may be some inconsistencies between this documentation and what is present on the machines.</p> <ol> <li>In cm UI, open Installed Patches to confirm which patches are currently installed. </li> <li>On raptor, there are several additional patches available that we will install on the cm. Patches are files with the <code>.sig</code> extension. <pre><code>ls -l /opt/guardium_tz_bootcamp_automation/upload/source_files/appliances/patches/\n</code></pre> </li> <li>The directory also contains a <code>patch_order.txt</code> file that specifies the correct order for applying patches. In the next step, you will provide a list of patches for installation - make sure to follow the installation sequence defined in this file. <pre><code>cat /opt/guardium_tz_bootcamp_automation/upload/source_files/appliances/patches/patch_order.txt\n</code></pre> </li> <li> <p>Login to cm cli and upload all patches to it using flow below. Execute command:</p> <pre><code>store system patch install scp\n</code></pre> <p>Insert:</p> <ul> <li>raptor IP address: raptor.demo.guardium</li> <li>user name: root</li> <li>patch path: <code>/opt/guardium_tz_bootcamp_automation/upload/source_files/appliances/patches/*.sig</code></li> <li>Port: 2223</li> <li>password for root account on raptor</li> </ul> <p>Finally, you will be asked for order of patch installation, it must be comma separated list based on patch numbering on the displayed list (for example 4,3,2,1), please use correct order provided in patch_order.txt file mentioned above. Some other patches will be on the list - ignore them, they are related to previous patch processes.</p> <p>Note</p> <p>You will be asked for patch 9997 reinstallation confirmation - accept this!</p> <p></p> </li> <li> <p>Monitor installation progress using the relevant patch status commands. Some patches temporarily stop backend services, so one view may show errors. Check the list of downloaded patches on cm <pre><code>show system patch available\n</code></pre> </p> </li> <li> <p>All freshly uploaded patches will be scheduled, and you can monitor the installation process using commands:     <pre><code>show system patch installed\n</code></pre>      and     <pre><code>show system patch status\n</code></pre>      The first command can produce errors if some patch installation tasks require stop collector backend (MySQL). In this case you can use the second one to check the progress.</p> <p>Note</p> <p>Process can take several minutes (depending on patches applied during this course instance) and you must wait that all scheduled patches will be installed successfully. Some patches can force collector restart and you must re-login.</p> </li> <li> <p>Confirm that all patches are installed. You can noticed warning at patch 9997 \u2013 you can ignore it. <pre><code>show system patch installed\n</code></pre> </p> </li> <li>It is good practice to schedule the re-installation of patch 9997 at the beginning of every appliance patching process to ensure that the latest version available on the cm is installed.</li> </ol>"},{"location":"v14/appliance/#collectors-patching","title":"Collectors patching","text":"<ol> <li>Patching the remaining appliances can be managed centrally from the cm. In the cm UI, open Patch Management.     From the Available patches section, select patch 9997 (first in the <code>patch_order.txt</code> list), then select your collectors in the Guardium systems section. Click Install, which opens a popup where you can schedule the patch installation.     Choose NOW for immediate deployment and confirm by clicking Install. A confirmation popup should appear indicating that the installation job has been successfully created.     </li> <li>Repeat these steps for all other patches mentioned in <code>patch_order.txt</code> list.</li> <li>To check the patch installation status, select one of the appliances and choose Show all installed patches. This will open a window displaying the list of patches on that system. You should see the newly scheduled patches listed with various installation statuses. Confirm that all patches have been installed.     </li> </ol>"},{"location":"v14/appliance/#collector-default-policy","title":"Collector default policy","text":"<ol> <li> <p>A newly deployed collector has a default policy assigned that ignores all activity. To start collecting traffic, you need to install a meaningful policy\u2014use the one previously imported.     In the cm UI, open Security Policies, filter the view to show only policies (uncheck Include templates), select Default bootcamp policy, and from the actions list choose Install.     In the popup, select your coll1.demo.guardium and choose the installation type Install and override, then confirm with OK. You should receive confirmation that the policy has been installed on the coll1.     Do not install this policy on other appliances, as they will be used for a different purposes.     </p> </li> <li> <p>Open Central management view on cm. Verify that we have our policy installed on collector coll1 and generic one is still applied on the rest appliances. </p> </li> </ol>"},{"location":"v14/appliance/#backup-configuration-profiles-optional","title":"Backup, configuration profiles (optional)","text":"<ol> <li> <p>To configure scheduled appliance backup open in cm GUI the System Backup view</p> <p>Use this information to fill in a form:</p> <p>Endpoint URL: s3.ams03.cloud-object-storage.appdomain.cloud</p> <p>Access Key ID: 2a20bcab1bfc4503bc5466cd549aba6e</p> <p>Secret access key:  &lt;value available from TZ env materials&gt;</p> <p></p> </li> <li> <p>Use Test connection button to check correctness of inserted values and then close pop-up message and Save configuration. Do not run the backup on the cm; we will do it in a moment on the coll1. </p> </li> <li> <p>In the centrally managed environment we can spread some configuration settings across managed appliances to avoid repetion of this same tasks in the large installation. Let\u2019s do this with backup configuration. Open Distribute Configuration Profiles view and add a new one. </p> </li> <li> <p>Insert the Name, for example \u2013 Backup configuration and press Next button </p> </li> <li> <p>In the What to distribute section create a new one and select System Backup </p> </li> <li> <p>Similar to backup configuration for cm, define this same bucket and Save configuration</p> <p>Abstract</p> <p>Endpoint URL: s3.ams03.cloud-object-storage.appdomain.cloud</p> <p>Access Key ID: 2a20bcab1bfc4503bc5466cd549aba6e</p> <p>Secret access key:  &lt;value available from TZ env materials&gt;</p> <p>Then press Bucket Name button and select bucket-2znplpxrvg22c3v bucket. Select only Configuration checkmark. </p> </li> <li> <p>Just created configuration asset will appear on the list. Press Next button </p> </li> <li>In the Where to distribute panel select All Collectors group and move it to Selected groups section. Then press Next button. </li> <li>Save configuration and press Run Now button to send configuration to the collector </li> <li>Monitor the progress of profile distribution. Finally the Review distribution results section should appear and confirm that coll1 and the rest of collectors are successfully synchronized. </li> <li>Login to coll1 UI and confirm that System Backup configuration has been successfully set. Press Run Once Now button </li> <li>You can check status of task in Aggregation/Archive Log report (on coll1) </li> <li>Ask instructor to display content of bucket </li> </ol>"},{"location":"v14/appliance/#guardcli-accounts-optional","title":"guardcli accounts (optional)","text":"<ol> <li>cli accounts are shared ones. To manage cli access accountable the Guardium introduce on each appliance the nine additional accounts from guardcli1 to guardcli9 (they have OTP set to guardium). Let\u2019s us configure accountable access to cm cli.</li> <li>From raptor connect to cm using guardcli1 shared account (instead cli one) and reset password from OTP \u201cguardium\u201d to desired one if you will asked for change (must be strong, you can notice a bug and could be forced to change password two times), so execute from raptor: <pre><code>ssh -l guardcli1 -p22 cm\n</code></pre> </li> <li> <p>Try to execute any administration command, for example:</p> <pre><code>show network int all\n</code></pre> <p></p> </li> <li> <p>You will be informed that you cannot execute commands before reauthentication using the named UI account. Then activate a full access to the CLI using command below (provide correct password for demo UI account). Then execute any administration command to confirm this ability now. <pre><code>set guiuser demo\n\nshow network int all\n</code></pre> </p> </li> <li> <p>Add to your Training dashboard on cm the Detailed Guardium User Activity Trail report. If Add Report button is inactive you must activate it by clicking Edit mode</p> <p></p> </li> <li> <p>Open just added report in the Dashboard on cm. Notice that your guardcli1 session is marked as belonging to demo UI account and we can track activity correctly. </p> </li> <li>On the fresh installed appliance all guardcliX accounts have the well known OTP password \u2013 guardium. The good administration approach is access control to all proxy accounts. A good practice is to block those accounts or change the password to prevent unauthorized login attempts. To list guardcliX account status execute in CM cli session: <pre><code>show guarduser_state all\n</code></pre></li> <li> <p>Disable all proxy accounts - except just used above the guardcli1 - by execution commands:</p> <pre><code>store guarduser_state disable &lt;guardcli[2-9]&gt;\n</code></pre> <p>Proxy account should be now set this way</p> <p></p> </li> </ol>"},{"location":"v14/appliance/#appendix","title":"Appendix","text":"<p>Dependencies:</p> <p>IBM Cloud URL for COS. It is COS bucket belonging to zibi. In case of self-managed training or led by another instructor the backup lab should refer to appropriate, accessible COS bucket.</p> <p>https://cloud.ibm.com/objectstorage/crn%3Av1%3Abluemix%3Apublic%3Acloud-object-storage%3Aglobal%3Aa%2F8d537ccde33a156ac84a2890733b572d%3A7748e344-6c18-405c-be45-50b4742ee67c%3A%3A</p> <p>Resources:</p> <p>Instructor notes:</p>"},{"location":"v14/atap/","title":"ATAP Configuration","text":""},{"location":"v14/atap/#install-stap","title":"Install STAP","text":"<ol> <li> <p>Install kernel-devel for currently used kernel. <pre><code>dnf install -y kernel-devel-$(uname -r)\n</code></pre></p> </li> <li> <p>Install the latest GIM client on raptor <pre><code>cd /opt/guardium_tz_bootcamp_automation/upload/source_files/agents/shell/\n\n./guard-bundle-GIM-12.2.2.0_r123489_v12_x_1-rhel-9-linux-x86_64.gim.sh -- --dir /opt/guardium --tapip &lt;raptor_ip&gt; --sqlguardip &lt;cm_ip&gt;\n</code></pre></p> </li> <li> <p>In cm UI deploy STAP 12.2.1.1_r123268_5. Rember to ucheck option to see all available releases and confirm successful deployment.  </p> </li> <li> <p>Then check in S-TAP Control view in coll1 UI that Postgres inspection engine (database instance) has been discovered by agent. Use  icon in row where raptor STAP appears and select Edit option. In the pop-up point Inspection engines and locate pgsql instance on the list.  </p> </li> </ol>"},{"location":"v14/atap/#check-encrypted-traffic-visibility","title":"Check encrypted traffic visibility","text":"<ol> <li> <p>From root session on raptor connect to Postgres instance as a tom user using non-encrypted connection and then check connection type and execute simple dummy SQL (use default environment password). <pre><code>psql \"postgresql://raptor.demo.guardium:5432/postgres?sslmode=disable\" -U tom -W\n\nSELECT * FROM pg_stat_ssl WHERE pid=(SELECT pid FROM pg_stat_activity WHERE query LIKE 'SELECT%');\n\nSELECT 'TOM\u2019s SESSION WITHOUT SSL';\n\n\\q\n</code></pre> Notice that column ssl in output from first query has value f (false) what means that connection is not encrypted  </p> </li> <li> <p>Open Full SQL (Training) report in Training dashboard on coll1.  </p> </li> <li> <p>Connect to Postgres instance as a jerry using encrypted connection. <pre><code>psql \"postgresql://raptor.demo.guardium:5432/postgres?sslmode=require\" -U jerry -W\n</code></pre></p> </li> <li> <p>Check connection parameters and execute dummy SQL. <pre><code>SELECT * FROM pg_stat_ssl WHERE pid=(SELECT pid FROM pg_stat_activity WHERE query LIKE 'SELECT%');\n\nSELECT 'JERRY\u2019s SESSION USING SSL';\n\n\\q\n</code></pre> </p> </li> <li> <p>Check Full SQL (Training) report again and notice that traffic related for encrypted session did not appear there. In case of encrypted connections the traffic visibility requires ATAP configuration (if available).</p> </li> </ol>"},{"location":"v14/atap/#atap-setup","title":"ATAP setup","text":"<ol> <li> <p>As a root user on raptor execute these commands to configure ATAP for Postgres instance (<code>guardctl</code> command requires full path specification during execution). <pre><code>/opt/guardium/modules/ATAP/current/files/bin/guardctl --db-user=postgres --db-home=/usr --db-user-dir=/var/lib/pgsql --db-type=postgres --db-instance=postgres --db-version=16 store-conf\n\n/opt/guardium/modules/ATAP/current/files/bin/guardctl authorize-user postgres\n</code></pre></p> </li> <li> <p>Stop Postgres database engine. <pre><code>systemctl stop postgresql\n</code></pre></p> </li> <li> <p>Activate ATAP. <pre><code>/opt/guardium/modules/ATAP/current/files/bin/guardctl --db-instance=postgres activate\n</code></pre> </p> </li> <li> <p>Start Postgres. <pre><code>systemctl start postgresql\n</code></pre></p> </li> <li> <p>Connect to database with SSL using a jerry account again. <pre><code>psql \"postgresql://raptor.demo.guardium:5432/postgres?sslmode=require\" -U jerry -W\n</code></pre></p> </li> <li> <p>Execute again SQL\u2019s inside encrypted session. <pre><code>SELECT * FROM pg_stat_ssl WHERE pid=(SELECT pid FROM pg_stat_activity WHERE query LIKE 'SELECT%');\n\nSELECT 'A NEW JERRY\u2019s SESSION USING SSL';\n\n\\q\n</code></pre></p> </li> <li> <p>Check activity in Full SQL (Training) report and confirm that this time jerry\u2019s session is correctly monitored. </p> </li> </ol>"},{"location":"v14/atap/#atap-upgrade","title":"ATAP upgrade","text":"<ol> <li> <p>In cm UI open Setup by Client view and configure STAP upgrade from 12.2.1.1 to 12.2.2.0. </p> </li> <li> <p>Monitor upgrade process and it should finish with success. This behaviour is expected from release 12.2 and up. Before 12.2 the agent upgrade required the ATAP deactivation. Now process of ATAP upgrade works in live. </p> </li> <li> <p>Login to Postgres as a tom using SSL channel on raptor. <pre><code>psql \"postgresql://raptor.demo.guardium:5432/postgres?sslmode=require\" -U tom -W\n</code></pre></p> </li> <li> <p>Execute some SQL\u2019s. <pre><code>SELECT * FROM pg_stat_ssl WHERE pid=(SELECT pid FROM pg_stat_activity WHERE query LIKE 'SELECT%');\n\nSELECT 'TOM\u2019s SESSION USING SSL AFTER STAP UPGRADE';\n\n\\q\n</code></pre></p> </li> <li> <p>Check Full SQL (Training) report on coll1 and confirm that just executed activity is correctly audited. </p> </li> </ol>"},{"location":"v14/atap/#stap-settings","title":"STAP settings","text":"<ol> <li> <p>Check on raptor connection to coll1. <pre><code>netstat -an | grep &lt;coll1_ip&gt;\n</code></pre>  Only one connection on port 16016 is established between STAP and coll1. This port is used for clear text comunication (NO TLS communication)</p> </li> <li> <p>Check SSL settings on port 16016 to confirm that there is no SSL handshake possible on this port <pre><code>openssl s_client -connect &lt;coll1_ip&gt;:16016\n</code></pre> </p> </li> <li> <p>Using Set up by Client view on cm modify STAP module parameters.</p> <p>Set parameters:</p> <ul> <li>STAP_USE_TLS: 1</li> <li>STAP_STATISTICS: -3</li> <li>STAP_CONNECTION_POOL_SIZE: 2</li> </ul> <p>Select currently installed version of STAP (12.2.2.0). Confirm that changes are applied.</p> <p></p> </li> <li> <p>Then check again connections list to coll1 from raptor. <pre><code>netstat -an | grep &lt;coll1_ip&gt;\n</code></pre>  You should now see three connections. Notice that the connection on port 16016 is no longer available, as communication has switched to the encrypted channel between the agent and the coll1 (16018). The two additional connections on port 16021 are also in use, corresponding to the two extra threads we configured.</p> </li> <li> <p>Test encryption status on these two ports (16018 and 16021) <pre><code>openssl s_client -connect &lt;collector_ip&gt;:16018 &lt; /dev/null\n\nopenssl s_client -connect &lt;collector_ip&gt;:16021 &lt; /dev/null\n</code></pre> STAP_STATISTIC parameter set to value -3 we will check later on. On heavily loaded systems you can consider use additional port to manage more connection between STAP and collector. In this case you must change value of STAP_CONNECTION_POOL_SIZE parameter from 0 to positive value.</p> </li> <li> <p>To stop STAP from GIM server set parameter STAP_ENABLED to 0. Notice that value 2 will force STAP restart.  </p> </li> <li> <p>Then check STAP status in S-TAP Control in the coll1 UI and confirm that STAP is stopped. </p> </li> <li> <p>Start STAP from command line on raptor. <pre><code>/opt/guardium/modules/STAP/current/guard-config-update --enable stap\n</code></pre></p> </li> <li> <p>Then when STAP will be again operating, edit a raptor STAP configuration in S-TAP Control view on coll1 \u2013 press  icon to select Edit option. It opens STAP configuration pop-up. Select Inspection engines from left menu and select all of them then delete all using Delete Inspection Engine in the options bar. It opens a warning window to Confirm desired changes. </p> </li> <li> <p>Now list of the Inspection engines should be empty. Save the changes. </p> </li> <li> <p>The STAP will be in Not synchronized state (yellow) because requested change is not yet consumed by STAP. </p> </li> <li> <p>If you refresh the status after a while the status should change to Configuration error state (pink) because lack of inspection engine definitions. To rediscover them open again menu using  icon and select Send commands option. It opens pop-up where one of the commands is Run database instance discovery. Select it and check the Replace Inspection Engines option. Then Apply changes. </p> </li> <li> <p>You back to S-TAP Control view and after a while (use refresh icon) the STAP status should change to Online because all IE\u2019s has been rediscovered.  </p> </li> <li> <p>We can do this same from command line on monitored machine what provides us also better understanding what is or is not discovered correctly. To re-discover inspection instances execute on raptor machine. <pre><code>/opt/guardium/modules/STAP/current/guard_discovery /opt/guardium/modules/STAP/current/guard_tap.ini --print_output \n</code></pre> </p> </li> <li> <p>Previous command produces list of identified DB instances on raptor and can modify the STAP configuration stored in <code>guard_tap.ini</code> file. Review the content of this file to identify DB_[X] sections. Each of them describes database instance which is monitored by STAP agent on raptor machine. <pre><code>cat /opt/guardium/modules/STAP/current/guard_tap.ini\n</code></pre> </p> </li> </ol>"},{"location":"v14/atap/#more-stap-features","title":"More STAP features","text":"<ol> <li> <p>Review the content of S-TAP and External S-TAP Statistics report in Dashboard on coll1, check columns S-TAP Total Packets Dropped, S-TAP Total Bytes Dropped, Stap CPU percent, Buffer recycled (if for some reasons the report will be empty try again change STAP_STATISTICS flag form -3 to -5 for example, continue the lab and check result in the meantime). </p> </li> <li> <p>Spend a while on statistics documentation to understand what mentioned in report above https://www.ibm.com/docs/en/guardium/12.x?topic=performance-linux-unix-s-tap-statistics</p> </li> <li> <p>From cm UI change raptor STAP settings and set STAP-UTILS_START_MONITOR value to Y. </p> </li> <li> <p>Check <code>/var/tmp/monitor</code> directory on raptor and notice that <code>guard_monitor.log</code> has been created there. <pre><code>cat /var/tmp/monitor/guard_monitor.log\n</code></pre></p> </li> <li> <p>Check location of STAP logs by searching for <code>tap_log_dir</code> parameter in <code>/opt/guardium/modules/STAP/current/guard_tap.ini</code> file on raptor. <pre><code>grep tap_log_dir /opt/guardium/modules/STAP/current/guard_tap.ini\n</code></pre> </p> </li> <li> <p>Notice that value is set to NULL, what means that <code>/tmp</code> directory is used to store main log file <code>guard_stap.stderr.txt</code>. Display log content. <pre><code>cat /tmp/guard_stap.stderr.txt\n</code></pre></p> </li> <li> <p>Create new directory where logs should be stored. <pre><code>mkdir -p /root/glogs\n</code></pre></p> </li> <li> <p>Modify <code>/opt/guardium/modules/STAP/current/guard_tap.ini</code> file and set variables.</p> <p>Set variables:</p> <ul> <li>tap_debug_output_level: 1</li> <li>tap_log_dir: <code>**/root/glogs**</code></li> </ul> <p></p> </li> <li> <p>Restart STAP. <pre><code>/opt/guardium/modules/STAP/current/guard-config-update --restart stap\n</code></pre></p> </li> <li> <p>Check content of <code>/root/glogs</code> directory and notice that logs are now created there. <pre><code>ls /root/glogs\n</code></pre> </p> </li> <li> <p>Check also S-TAP Events report on coll1 and notice that events are there (sort report using Timestamp column, click column name). </p> </li> <li> <p>Change debug logging level on raptor using <code>guard-config-update</code> back to value 0. <pre><code>/opt/guardium/modules/STAP/current/guard-config-update --modify-tap tap_debug_output_level 0\n</code></pre></p> </li> <li> <p>We can create STAP diagnostics from coll1 UI. In the S-TAP Control view press  icon and select Send commands option. Then \u2013 in the pop-up - select Run diagnostics command and set Debugging Level to 4 and Apply request.  </p> </li> <li> <p>After 2-3 minutes go to Support information gathering view and download requested diag file. </p> </li> <li> <p>Try to uninstall GIM client on raptor using <code>uninstall.pl</code> and notice that GIM client is aware that ATAP is actived and will ignore request. <pre><code>/opt/guardium/modules/GIM/current/uninstall.pl\n</code></pre> </p> </li> </ol>"},{"location":"v14/atap/#simple-inspection-engine-verification-optional","title":"Simple Inspection Engine Verification (optional)","text":"<ol> <li> <p>From cli on coll1 set the inspection engine verification timeout to 10 seconds. <pre><code>store stap network_latency 10\n</code></pre></p> </li> <li> <p>In the coll1 UI, locate and open the S-TAP Status Monitor view. This view displays the list of S-TAP agents connected to the coll1. In our case, it shows the agent installed on the raptor machine. Click the row containing the agent information. </p> </li> <li> <p>A list of Inspection Engines should appear. Select the one that corresponds to the pgsql on port 5432 (postgres) database. This will activate the list of available actions. Choose Verify. After a moment, you should receive a message indicating that the verification completed successfully. If the result is different, try running the verification again.  </p> </li> <li> <p>Let\u2019s now stop the postgres instance on raptor. <pre><code>systemctl stop postgresql\n</code></pre></p> </li> <li> <p>Try running the verification again. </p> </li> <li> <p>This time, the verification failed, and you can analyze the cause by using the Run Diagnostics option. </p> </li> <li> <p>After a moment, information about the possible causes of the failure will be displayed. As you can see, the STAP agent is running and the coll1 can communicate with it, but the issue is related to the lack of connection to the database. This is expected, since we previously stopped the database instance. </p> </li> <li> <p>Start postgres again on raptor. <pre><code>systemctl start postgresql\n</code></pre></p> </li> <li> <p>Run verification again and confirm that it works. Sometimes manually executed process of verification can fail without visible reasons. Try running diagnostics and probably will finish successfully. It should not happen if you schedule verification process.</p> </li> <li> <p>Check Failed Login Attempts view to realize that simple verification process uses the RESULTFD fake account to generate exception error. </p> </li> <li> <p>To configure the advances verification select pgsql instance again in S-TAP Status Monitor and press Advanced Verify. In pop-up window use plus icon to add data source definition. </p> </li> <li> <p>Configure access to postgres database.</p> <p>Form values:</p> <ul> <li>Name: postgres verification (raptor)</li> <li>Database type: PostgreSQL</li> <li>User name: tom</li> <li>Password: &lt;default password&gt;</li> <li>Host name/IP: raptor.demo.guardium</li> <li>Port number: 5432</li> <li>Database: postgres</li> </ul> <p>Then use Save, Test connection and Close buttons to store new definition and do the verification.</p> <p></p> </li> <li> <p>You should return to the Advanced Verification window with the name of the newly created data source already populated. Click Verify and wait for confirmation that traffic is visible on the monitored system. </p> </li> <li> <p>Finally, verify that this time the instance validation generated an SQL error by executing a query against a non-existent table. Check the Full SQL (Training) and SQL Errors reports on the dashboard.  </p> </li> </ol>"},{"location":"v14/atap/#atap-for-mongo-optional","title":"ATAP for Mongo (optional)","text":"<ol> <li> <p>On the raptor machine, a MongoDB database is running and listening on port 27017. <pre><code>lsof -i -P -n | grep 27017\n</code></pre></p> </li> <li> <p>Let\u2019s manage some configuration tasks from cli instead of UI. In the coll1 cli session list the inspection engines on raptor. <pre><code>grdapi list_inspection_engines stapHost=raptor.demo.guardium\n</code></pre> </p> </li> <li> <p>Notice that the current inpection engine definitions for MySQL uses the port range 3306\u201333060, which unintentionally includes the port designated for the mongo instance (27017). To avoid this conflict, the MySQL inspection engine definitions should be adjusted to target only the specific ports required, instead of relying on a range.  </p> </li> <li> <p>So, execute from coll1 cli: <pre><code>grdapi delete_stap_inspection_engine type=mysql stapHost=raptor.demo.guardium\n\ngrdapi create_stap_inspection_engine dbUser=mysqld dbVersion=8 client=0.0.0.0/0.0.0.0 ktapDbPort=3306 portMin=3306 portMax=3306 protocol=mysql procName=/usr/sbin/mysqld dbInstallDir=/var/lib/mysql unixSocketMarker=mysql.sock stapHost=raptor.demo.guardium\n\ngrdapi create_stap_inspection_engine dbUser=mysqld dbVersion=8 client=0.0.0.0/0.0.0.0 ktapDbPort=33060 portMin=33060 portMax=33060 protocol=mysql procName=/usr/sbin/mysqld dbInstallDir=/var/lib/mysql unixSocketMarker=mysql.sock stapHost=raptor.demo.guardium\n\ngrdapi create_stap_inspection_engine dbUser=mysqld dbVersion=8 client=0.0.0.0/0.0.0.0 ktapDbPort=3306 portMin=3306 portMax=3306 protocol=mysql procName=/usr/sbin/mysqld dbInstallDir=/var/lib/mysql unixSocketMarker=mysqlx.sock stapHost=raptor.demo.guardium\n\ngrdapi create_stap_inspection_engine dbUser=mysqld dbVersion=8 client=0.0.0.0/0.0.0.0 ktapDbPort=33060 portMin=33060 portMax=33060 protocol=mysql procName=/usr/sbin/mysqld dbInstallDir=/var/lib/mysql unixSocketMarker=mysqlx.sock stapHost=raptor.demo.guardium\n\ngrdapi list_inspection_engines stapHost=raptor.demo.guardium\n</code></pre>  We have recreated the mysql\u2019s inspection engines to point one port per instance.</p> </li> <li> <p>Generate mongo traffic on raptor and check Full SQL (Training) report on coll1. Activity should be audited correctly. <pre><code>mongosh $MONGO_URI\n\nshow dbs\n\nuse sample_mflix\n\nshow tables\n\ndb.movies.find()\n\nexit\n</code></pre> </p> </li> <li> <p>The MongoDB instance is configured to use encrypted connections. Connect using this method, execute a few queries, and then verify that the activity is not captured for this session. <pre><code>mongosh --tls --tlsAllowInvalidCertificates $MONGO_URI\n\nshow dbs\n\nuse sample_mflix\n\nshow tables\n\ndb.theaters.find()\n\nexit\n</code></pre></p> </li> <li> <p>Configure ATAP for mongo. <pre><code>/opt/guardium/modules/ATAP/current/files/bin/guardctl authorize-user mongod\n\n/opt/guardium/modules/ATAP/current/files/bin/guardctl --db-type=mongodb --db-instance=mongo4 --db-user=mongod --db-home=/usr --db-base=/var/lib/mongo store-conf\n\nsystemctl stop mongod\n\n/opt/guardium/modules/ATAP/current/files/bin/guardctl --db-instance=mongo4 activate\n\nsystemctl start mongod\n</code></pre></p> </li> <li> <p>Generate traffic again in the encrypted connection and confirm that mongo traffic is audited correctly. <pre><code>mongosh --tls --tlsAllowInvalidCertificates $MONGO_URI\n\nshow dbs\n\nuse sample_mflix\n\nshow tables\n\ndb.theaters.find()\n\nexit\n</code></pre> </p> </li> <li> <p>To avoid the deleted inspection engine recreation by STAP let us disable this functionality on raptor. From cm UI in Setup by client select installed agent (12.2.2.0_r123489_3) and set these parameters:</p> <p>STAP parameters:</p> <ul> <li>STAP_DISCOVERY_ENABLED: 0</li> <li>STAP_DISCOVERY_DBS: oracle:db2:informix:postgres:sybase:teradata:netezza:memsql:mariadb:verticadb:mongodb</li> </ul> <p></p> </li> </ol>"},{"location":"v14/atap/#appendix","title":"Appendix","text":"<p>Dependencies:</p> <p>It is crucial to finish all non-optional parts to be able continue.</p> <p>Resources:</p> <ol> <li>ATAP documentation   https://www.ibm.com/docs/en/gdp/12.x?topic=management-linux-unix-database-specific-guardctl-parameters</li> </ol> <p>Instructor notes:</p>"},{"location":"v14/etap/","title":"External S-TAP configuration","text":""},{"location":"v14/etap/#mysql-traffic-visibility","title":"MYSQL Traffic visibility","text":"<ol> <li>One of the databases working on raptor machine is MySQL. Let\u2019s check connectivity and confirm MySQL session audibility. Login to the raptor and execute command: <pre><code>mysql\n</code></pre></li> <li> <p>Inside MySQL session execute (first command reveal information that session is not encrypted): <pre><code>SHOW SESSION STATUS LIKE 'Ssl_cipher';\n\nSELECT 'PLAINTEXT SESSION';\n\nexit;\n</code></pre> </p> </li> <li> <p>Login again  but this time refer to IP address of raptor machine. <pre><code>mysql -h raptor.demo.guardium\n</code></pre></p> </li> <li> <p>Execute these commands in the session (notice, that session is encrypted): <pre><code>SHOW SESSION STATUS LIKE 'Ssl_cipher';\n\nSELECT 'ENCRYPTED SESSION SESSION';\n\nexit;\n</code></pre> </p> </li> <li> <p>Go to Full SQL (Training) report in coll1 dashboard and notice that only SQL\u2019s related to plaintext session have been audited. </p> </li> <li> <p>There is no ATAP or EXIT for MySQL. In case of encrypted traffic for MySQL we need to use different method of monitoring which is not based on STAP.</p> </li> </ol>"},{"location":"v14/etap/#docker-preparation","title":"Docker preparation","text":"<ol> <li> <p>Install <code>podman-docker</code> and <code>skopeo</code> packages. <pre><code>dnf -y install podman-docker skopeo\n</code></pre></p> </li> <li> <p>To check the latest available tag of External S-TAP image execute: <pre><code>skopeo list-tags docker://icr.io/guardium/guardium_external_s-tap\n</code></pre> </p> </li> <li> <p>Pull External S-TAP image with the latest tag for Guardium 12.2 (screenshots presents example, use the latest image version). <pre><code>docker pull icr.io/guardium/guardium_external_s-tap:v&lt;latest_12.2&gt;\n</code></pre> </p> </li> <li> <p>List local images and write in notebook container name and tag <pre><code>podman images\n</code></pre> </p> </li> </ol>"},{"location":"v14/etap/#ssl-certificate-setup","title":"SSL certificate setup","text":"<ol> <li> <p>On raptor create directory for CA, which we will deploy to serve External S-TAP (ETAP) demands. <pre><code>mkdir -p /opt/ETAP/ca\n</code></pre></p> </li> <li> <p>Login to coll1 cli and request certificate for ETAP instance (CSR request) using command:     <pre><code>create csr external_stap\n</code></pre></p> <p>Insert certificate request parameters:</p> <ul> <li>unique alias: raptor-mysql-etap</li> <li>etap certificate common name: mysqlcoll1.demo.guardium</li> <li>organization unit name: Demo</li> <li>add additional OU: n</li> <li>organization name: Guardium</li> <li>city: &lt;your city name&gt;</li> <li>province: &lt;your province name&gt;</li> <li>country code: &lt;your 2-digits country code&gt;</li> <li>email of collector certificate owner, can be fake: &lt;email_address&gt;</li> <li>accept default algorithm: Enter</li> <li>accept default key length: Enter</li> <li>insert FQDN of your collector for SAN #1: coll1.demo.guardium</li> <li>press Enter for SAN #2: Enter</li> </ul> <p></p> <p>Then certificate request will be created and displayed</p> <p></p> </li> <li> <p>Then copy displayed Certificate Request to file on raptor - <code>/opt/ETAP/ca/etap.csr</code> and write down in notepad entire alias and related to it token (two bottom lines)     </p> </li> <li> <p>Now we will create new certification authority (CA) on raptor. Go to directory <code>/opt/ETAP/ca</code> (where you saved <code>etap.csr</code> file) and create RSA key. <pre><code>cd /opt/ETAP/ca\n\nopenssl genrsa -out ca.key 2048\n</code></pre> </p> </li> <li> <p>Create CA certificate, insert required certificate parameters</p> <pre><code>openssl req -x509 -sha256 -new -key ca.key -days 3650 -out ca.pem\n</code></pre> <p>Insert these values:</p> <ul> <li>Country Name: &lt;your 2-digits country code&gt;</li> <li>Province: &lt;your province name&gt;</li> <li>City: &lt;your city name&gt;</li> <li>Organization: Guardium</li> <li>Organization Unit: Demo</li> <li>Common name: etap_ca</li> <li>e-mail of CA owner, can be fake one: &lt;email_address&gt;</li> </ul> <p></p> <p>In the current directory <code>ca.pem</code> certificate file will appear.</p> </li> <li> <p>Sign certificate request from coll1 using just created CA certificate. <pre><code>openssl x509 -sha256 -req -days 3650 -CA ca.pem -CAkey ca.key -CAcreateserial -CAserial serial -in etap.csr -out etap.pem\n</code></pre>  In the current directory certificate based on CSR generated on coll1 will appear in <code>etap.pem</code> file.</p> </li> <li> <p>Install CA certificate on coll1. Provide unique name for CA (for example: etap_ca) and then insert content of <code>ca.pem</code> file from <code>/opt/ETAP/ca</code> directory. Open two SSH sessions to raptor, one to get access to certificates and second where you are logged to coll1, it will simplify certificate injection. <pre><code>store certificate keystore_external_stap\n</code></pre>   Then insert Ctrl+D keystroke.</p> </li> <li> <p>You should receive confirmation that CA cert has been imported.  </p> </li> <li> <p>Now we will import to the coll1 the External S-TAP certificate - <code>etap.pem</code> file stored in <code>/opt/ETAP/ca</code>. You must provide as an alias the one generated during CSR request in point 3. <pre><code>store certificate external_stap\n</code></pre> </p> </li> <li> <p>Confirm that import finished with success.  </p> </li> <li> <p>Execute command below to list ETAP certs stored on coll1. Confirm installation of CA and ETAP cert. <pre><code>show certificate external_stap\n</code></pre> </p> </li> </ol>"},{"location":"v14/etap/#deploy-etap","title":"Deploy ETAP","text":"<ol> <li> <p>On raptor go to ETAP lab directory and clone External S-TAP gitgub project. <pre><code>cd /opt/ETAP\n\ngit clone https://github.com/IBM/Guardium_External_S-TAP.git\n</code></pre> </p> </li> <li> <p>The script used to create the container unfortunately does not support usage of SSH on port other than 22, so temporarily add this port to the sshd service on raptor. Edit <code>/etc/ssh/ssh_config.d/99-cluster-hosts.conf</code> and add an additional entry Port 22 below the existing Port 2223 for line describing the Host with raptor IP address. And below add one more section which will describe the localhost as well. <pre><code>Host &lt;your_raptor_ip&gt;\n    Port 2223\n    Port 22\n    StrictHostKeyChecking no\n    UserKnownHostsFile /dev/null\n    ForwardAgent yes\n\nHost localhost\n    Port 2223\n    Port 22\n    StrictHostKeyChecking no\n    UserKnownHostsFile /dev/null\n    ForwardAgent yes\n</code></pre> </p> </li> <li> <p>Restart sshd service. <pre><code>systemctl restart sshd\n</code></pre></p> </li> <li> <p>Set some variables to deploy ETAP silenty and minimize mistakes in the container setup. <pre><code>STATE_FILE=mysql_etap_state\n\nDB_HOST=&lt;raptor_ip&gt;\n\nCOLLECTOR=&lt;coll1_ip&gt;\n\nIMAGE='icr.io/guardium/guardium_external_s-tap:v&lt;latest_12.2&gt;'\n\nTOKEN=&lt;token displayed in point 3 of SSL certificate setup section&gt;\n\nDB_PORT=3306\n\nDB_TYPE=mysql\n\nPROXY_WORKERS=1\n\nPROXY_PROTOCOL=0\n\nPORT_RANGE='63333-63333'\n\nTENANT=MYSQLETAP\n\nLBMODE=0\n</code></pre> </p> </li> <li> <p>Go to <code>Guardium_External_S-TAP</code> directory and start ETAP deployment script. The command will generate a long output, but the key point is to confirm at the end that the new container has started successfully. <pre><code>cd /opt/ETAP/Guardium_External_S-TAP\n\n./container_mgmt.sh --state-file $STATE_FILE --db-host $DB_HOST --proxy-num-workers $PROXY_WORKERS --proxy-protocol $PROXY_PROTOCOL --db-port $DB_PORT --proxy-secret $TOKEN --db-type $DB_TYPE --sqlguard-ip $COLLECTOR --participate-in-load-balancing $LBMODE --tenant-id $TENANT --svc-image $IMAGE --svc-port-range $PORT_RANGE --ni --c\n</code></pre> </p> </li> <li> <p>Check container instance status <pre><code>podman ps\n</code></pre> </p> </li> <li> <p>Login to coll1 UI and open External S-TAP Instances view and confirm that just deployed instance is correctly registered on coll1.  </p> </li> </ol>"},{"location":"v14/etap/#check-traffic-visibility","title":"Check traffic visibility","text":"<ol> <li> <p>Login to MySQL using External S-TAP proxy port and execute some SQL\u2019s <pre><code>mysql -P 63333 -h raptor.demo.guardium\n</code></pre></p> </li> <li> <p>Execute some SQL\u2019s <pre><code>SHOW SESSION STATUS LIKE 'Ssl_cipher';\n\nSELECT 'ENCRYPTED SESSION ON ETAP PORT 63333';\n\nexit\n</code></pre> </p> </li> <li> <p>Check activity in Full SQL (Training) report on coll1.   </p> </li> </ol>"},{"location":"v14/etap/#automatic-start-of-etap-instance-optional","title":"Automatic start of ETAP instance (optional)","text":"<ol> <li> <p>We used the script to create the ETAP container, but it will not start automatically the ETAP after a system reboot. Use the quadlet mechanism available in podman to ensure the ETAP container is persistently available and easily manageable using standard Linux system services. Before starting ETAP using system services, remove the currently running instance. Display the container details and record its ID. <pre><code>podman ps\n</code></pre> </p> </li> <li> <p>Stop the container and remove it. <pre><code>podman stop &lt;container_id&gt;\n\npodman rm &lt;container_id&gt;\n</code></pre></p> </li> <li> <p>Copy the service template from the helper directory to the Podman services directory. <pre><code>cp /opt/guardium_tz_bootcamp_automation/upload/source_files/mysql/mysql_external_stap.container /etc/containers/systemd/mysql_etap.container\n</code></pre></p> </li> <li> <p>Edit just copied file - <code>/etc/containers/systemd/mysql_etap.container</code> and set some values.</p> <p>Values to modify:</p> <p>&lt;latest_etap_release&gt;: defined in Docker preparation point 2 &lt;proxy_secret&gt;: token displayed in point 3 od SSL certificate setup &lt;coll1_IP_address&gt;: coll1 IP address &lt;raptor_IP_address&gt;: raptor IP address</p> <p> </p> </li> <li> <p>Reload the network services configuration. <pre><code>systemctl daemon-reload\n</code></pre></p> </li> <li> <p>Start ETAP service. <pre><code>systemctl start mysql_etap\n</code></pre></p> </li> <li> <p>Check whether the container is running. <pre><code>podman ps\n</code></pre> </p> </li> <li> <p>Finally, connect to the database through ETAP and verify that the activity is being monitored. <pre><code>mysql -P 63333 -h raptor.demo.guardium\n</code></pre></p> </li> <li> <p>Execute some SQL\u2019s <pre><code>SHOW SESSION STATUS LIKE 'Ssl_cipher';\n\nSELECT 'ENCRYPTED SESSION ON ETAP PORT 63333 \u2013 QUADLET SERVICE';\n\nexit\n</code></pre> </p> </li> </ol>"},{"location":"v14/etap/#appendix","title":"Appendix","text":"<p>Dependencies:</p> <p>This lab is required for the Oracle lab and should be completed.</p> <p>Resources:</p> <p>Instructor notes:</p>"},{"location":"v14/exit/","title":"EXIT configuration","text":""},{"location":"v14/exit/#db2-traffic-visibility-with-ktap","title":"DB2 traffic visibility with KTAP","text":"<ol> <li> <p>One of databases deployed on raptor is DB2. It is not configured to support encrypted connections. However for some databases the KTAP is not enough to sniff non-TCP/IP stack based connections. Let\u2019s check this kind of case. Change context to db2inst1 user on raptor. <pre><code>su - db2inst1\n</code></pre></p> </li> <li> <p>List configured connections types can be displayed using command <pre><code>db2 list database directory\n</code></pre> Notice that database SAMPLE can be referred (connected) using Remote (TCP/IP, mysample) or Indirect (Shared Memory, sample) alias.</p> </li> <li> <p>Let\u2019s connect to sample database as a db2inst1 user usinng TCP connection (sampledb alias) <pre><code>db2 connect to mysample user db2inst1 \n\ndb2 \"SELECT 'TCP/IP connection test' FROM sysibm.sysdummy1\"\n</code></pre></p> </li> <li> <p>Then, connect to sample using Shared Memory connection (sample alias) <pre><code>db2 connect to sample user db2inst1 \n\ndb2 \"SELECT 'Shared Memory connection test' FROM sysibm.sysdummy1\"\n\nexit\n</code></pre></p> </li> <li> <p>Check traffic visibility in Full SQL (Training) report on coll1. Notice that activity from shared memory connection is not audited.  </p> </li> </ol>"},{"location":"v14/exit/#configure-db2-system-with-exit","title":"Configure DB2 system with EXIT","text":"<ol> <li> <p>As a root user on raptor authorize db2inst1 user to send events to STAP. <pre><code>/opt/guardium/modules/ATAP/current/files/bin/guardctl authorize-user db2inst1\n</code></pre></p> </li> <li> <p>As a db2inst1 user on raptor stop DB2 instance. <pre><code>su - db2inst1\n\ndb2stop\n</code></pre></p> </li> <li> <p>If command will not work you can force it using this one. <pre><code>db2stop force\n</code></pre></p> </li> <li> <p>Create directory for EXIT plugin in DB2 home directory. <pre><code>mkdir -p /home/db2inst1/sqllib/security64/plugin/commexit\n</code></pre></p> </li> <li> <p>Link EXIT plugin to DB2 directory. <pre><code>ln -fs /usr/lib64/libguard_db2_exit_64.so /home/db2inst1/sqllib/security64/plugin/commexit/libguard_db2_exit_64.so\n</code></pre></p> </li> <li> <p>Update DB2 configuration to load Guardium EXIT library during engine start. <pre><code>db2 update dbm cfg using comm_exit_list libguard_db2_exit_64\n</code></pre></p> </li> <li> <p>Check that plugin has been correctly registered using command below. The interesting us line should be at the end of command output. <pre><code>db2 get database manager configuration\n</code></pre> </p> </li> <li> <p>Start DB2 engine. <pre><code>db2start\n\nexit\n</code></pre></p> </li> <li> <p>Now we must re-create existing Inspection Engine for DB2 to adapt it to using EXIT data stream instead of KTAP one. We can do it directly from UI by removing old IE definition and adding a new one in S-TAP Control or execute API call. There is also possibility to execute <code>setup_exit.sh</code> script on machine where database and STAP is installed. Let's use a script. Execute on raptor as a root. <pre><code>/opt/guardium/modules/STAP/current/setup_exit.sh db2\n</code></pre> Script will require two answers \u2013 Y and N respectively.</p> <p> </p> </li> <li> <p>Restart STAP. <pre><code>/opt/guardium/modules/STAP/current/guard-config-update --restart stap\n</code></pre></p> </li> <li>Now, DB2 is configured to use EXIT. We should even disable KTAP to avoid loading it to kernel space. However, we have on raptor many different databases that is why we do not want to disable KTAP and ATAP functionality for now.</li> </ol>"},{"location":"v14/exit/#db2-traffic-visibility-with-exit","title":"DB2 traffic visibility with EXIT","text":"<ol> <li> <p>Login to raptor as db2inst1 user and connect to DB2 using shared memory connection <pre><code>su - db2inst1\n\ndb2 connect to sample user db2inst1 \n\ndb2 \"SELECT 'Shared Memory SECOND connection test with EXIT' FROM sysibm.sysdummy1\"\n\nexit\n</code></pre></p> </li> <li> <p>Check Full SQL (Training) report and notice db2inst1 user command has been sniffed. </p> </li> </ol>"},{"location":"v14/exit/#informix-monitoring-with-exit-optional","title":"Informix monitoring with EXIT (optional)","text":"<ol> <li> <p>Start informix instance on raptor machine as a root user. <pre><code>systemctl start informix-ifxserver\n</code></pre></p> </li> <li> <p>Change context to informix user. <pre><code>su - informix\n</code></pre></p> </li> <li> <p>Connect to the database instance as a super admin. Notice that the session is established using a soctcp connection, which indicates plain, unencrypted communication without SSL/TLS. <pre><code>dbaccess sysmaster -\n\nSELECT sid, TRIM(net_client_name) AS client_protocol, net_client_type, net_protocol, net_options, net_read_cnt, net_write_cnt FROM sysnetworkio WHERE sid = DBINFO('sessionid');\n\nSELECT 'INFORMIX session without SSL';\n</code></pre> Close database session inserting Ctrl+D </p> </li> <li> <p>Confirm traffic visibility in Full SQL (Training) report on coll1. </p> </li> <li> <p>Connect to Informix again but this time using encrypted channel. The first <code>SELECT</code> indicates that the connection is using the socssl protocol, which means the communication is encrypted using SSL/TLS. As expected, activity from this session will not be reported, because EXIT must be enabled to monitor encrypted connections. <pre><code>export INFORMIXSERVER=ifxserver_ssl\n\ndbaccess sysmaster -\n\nSELECT sid, TRIM(net_client_name) AS client_protocol, net_client_type, net_protocol, net_options, net_read_cnt, net_write_cnt FROM sysnetworkio WHERE sid = DBINFO('sessionid');\n\nSELECT 'INFORMIX session with SSL';\n</code></pre> Close database session inserting Ctrl+D and <code>exit</code> from the informix user session.  </p> </li> <li> <p>To configure EXIT, first switch to the root user and add the informix user to the guardium group. <pre><code>/opt/guardium/modules/ATAP/current/files/bin/guardctl authorize-user informix\n</code></pre></p> </li> <li> <p>Execute the following commands in the context of the informix user. Create a symbolic link to the library in the database instance's <code>lib</code> directory. <pre><code>su - informix\n\nln -fs /usr/lib64/libguard_informix_exit_64.so $INFORMIXDIR/lib/libguard_informix_exit_64.so\n</code></pre></p> </li> <li> <p>An then you must enable EXIT manually because recent Informix versions contain a defect that prevents the use of the script previously used for the DB2 database. First, as a informix user context, create a configuration file - <code>$INFORMIXDIR/etc/ifxguard.ifxserver</code> - for standard unencrypted TCP connections and populate it with the following contents: <pre><code>NAME   ifxguard.ifxserver\nWORKERS   6\nLIBPATH   /opt/ibm/informix/lib/libguard_informix_exit_64.so\nDEBUG   2\nLOGFILE   /tmp/ifxserver.log\n</code></pre> </p> </li> <li> <p>Next, create a configuration file for encrypted connections - <code>$INFORMIXDIR/etc/ifxguard.ifxserver_ssl</code> and populate it with the following contents: <pre><code>NAME   ifxguard.ifxserver_ssl\nWORKERS   6\nLIBPATH   /opt/ibm/informix/lib/libguard_informix_exit_64.so\nDEBUG   2\nLOGFILE   /tmp/ifxserver_ssl.log\n</code></pre> </p> </li> <li> <p>You can now start the EXIT services for each configuration (still in the informix user context). <pre><code>ifxguard -c $INFORMIXDIR/etc/ifxguard.ifxserver\n\nifxguard -c $INFORMIXDIR/etc/ifxguard.ifxserver_ssl\n</code></pre> </p> </li> <li> <p>You must remove the Informix KTAP-based inspection engine definition from the STAP and add the correct configuration required for EXIT monitoring. To do this, use the API. From the cli session on coll1, execute the following command: <pre><code>grdapi delete_stap_inspection_engine type=informix stapHost=raptor.demo.guardium\n\ngrdapi create_stap_inspection_engine dbUser=informix dbVersion=15 client=0.0.0.0/0.0.0.0 protocol=\"Informix Exit\" procName=/opt/ibm/informix/bin/oninit dbInstallDir=/home/informix stapHost=raptor.demo.guardium\n</code></pre></p> </li> <li> <p>Once again, switch to the informix user on raptor, connect to the instance using SSL, and execute a query. This time, the activity should be properly audited and visible in Guardium. <pre><code>export INFORMIXSERVER=ifxserver_ssl\n\ndbaccess sysmaster -\n\nSELECT sid, TRIM(net_client_name) AS client_protocol, net_client_type, net_protocol, net_options, net_read_cnt, net_write_cnt FROM sysnetworkio WHERE sid = DBINFO('sessionid');\n\nSELECT 'Second try of INFORMIX session with SSL';\n</code></pre> Close database session inserting Ctrl+D and <code>exit</code> from informix user session.  </p> </li> <li> <p>Stop Informix instance at the end of lab to free a raptor resources. As a root execute: <pre><code>systemctl stop informix-ifxserver\n</code></pre></p> </li> </ol>"},{"location":"v14/exit/#appendix","title":"Appendix","text":"<p>Dependencies:</p> <p>This lab deliverables are not referred in the other labs. So you can ignore it if needed.</p> <p>Resources:</p> <p>Instructor notes:</p>"},{"location":"v14/gim/","title":"GIM","text":""},{"location":"v14/gim/#gim-minimum-installation-and-de-installation","title":"GIM minimum installation and de-installation","text":"<ol> <li>Because this is a freshly installed environment, the raptor machine must be rebooted. Once the reboot is complete, sign in to the system again. <pre><code>shutdown -r now\n</code></pre></li> <li>On raptor using ssh, jump to directory <code>/opt/guardium_tz_bootcamp_automation/upload/source_files/agents/shell/</code> <pre><code>cd /opt/guardium_tz_bootcamp_automation/upload/source_files/agents/shell/\n</code></pre></li> <li>Set all files as a executable <pre><code>chmod +x *.sh\n</code></pre></li> <li>Execute GIM client installer without parameters to see available installation parameters <pre><code>./guard-bundle-GIM-12.2.1.1_r123268_v12_x_1-rhel-9-linux-x86_64.gim.sh \n</code></pre> </li> <li>Install GIM client in the listener mode <pre><code>./guard-bundle-GIM-12.2.1.1_r123268_v12_x_1-rhel-9-linux-x86_64.gim.sh -- --dir /opt/guardium --tapip &lt;raptor_ip&gt;\n</code></pre> </li> <li>Open port 8445 on raptor and check socket availability of this port. <pre><code>firewall-cmd --zone=public --add-port=8445/tcp\n\nfirewall-cmd --reload\n\nnc -zv &lt;raptor_ip&gt; 8445\n</code></pre> </li> <li> <p>You can also list GIM client processes on raptor machine and notice that it is listening on port 8445. <pre><code>ps -ef | grep gim\n</code></pre> </p> </li> <li> <p>On cm, using UI check the absence of GIM clients currently registered to (GIM Process Monitor form). </p> </li> <li> <p>Then register installed gim_client using GIM Remote Activation form. Put the raptor IP address and listener default port (8445) and press Submit button. The pop-up window should confim that gim client has been successfully registered to GIM server. </p> </li> <li> <p>Check again GIM Process Monitor view and notice that raptor GIM client is registered. </p> </li> <li> <p>In most cases, GIM client will not be installed in listener mode; instead, the connection to the GIM server will be configured during installation. To proceed, first uninstall the GIM client. Run the uninstallation command on the raptor machine.  <pre><code>/opt/guardium/modules/GIM/current/uninstall.pl\n</code></pre> </p> </li> <li> <p>Check GIM Process Monitor and notice that list of GIM clients is empty.</p> </li> <li> <p>Then install GIM client again on raptor with automatic registration to GIM server located on your cm. <pre><code>cd /opt/guardium_tz_bootcamp_automation/upload/source_files/agents/shell/\n\n./guard-bundle-GIM-12.2.1.1_r123268_v12_x_1-rhel-9-linux-x86_64.gim.sh -- --dir /opt/guardium --tapip &lt;raptor_ip&gt; --sqlguardip &lt;cm_ip&gt;\n</code></pre> </p> </li> <li> <p>Switch to cm UI (your GIM server) and check view \u2013 GIM Processes Monitor to confirm that GIM client has been registered successfully again. </p> </li> </ol>"},{"location":"v14/gim/#gim-modules-upload","title":"GIM modules upload","text":"<ol> <li> <p>Now we need to download GIM modules to GIM server. They are unpacked on ceratops. On ceratops machine (RDP connection) open Edge browser and ignore browser personalization (remember to open RDP tunnel) </p> </li> <li> <p>Login to cm UI from ceratops (you should have cm bookmark in favourites inside Guardium group) then open Upload Modules view  </p> </li> <li> <p>Click plus  button to upload the modules <code>c:\\bootcamp\\gim</code> directory. Select the first file. Once selected, click Upload.</p> <p>Important</p> <p>Do not refresh the window. Wait patiently till the import confirmation will appear. When it does, click Import Now and wait until you see a message confirming that the import completed successfully.</p> <p>  1.  Import module </p> </li> <li> <p>Confirm that GIM module has been uploaded </p> </li> <li> <p>Repeat steps 2-4 for all files in <code>c:\\bootcamp\\gim</code> directory!</p> <p>Info</p> <p>Your GIM modules list can be different than presented on screenshot.</p> <p></p> </li> </ol>"},{"location":"v14/gim/#gim-upgrade","title":"GIM upgrade","text":"<ol> <li> <p>Go to Set up by Client in cm UI and select your raptor GIM client. Switch to next section by pressing Next button </p> </li> <li> <p>Expand list of modules and select from the list BUNDLE-GIM module. Notice that the installed version of GIM client is for Guardium release 12.1 and the latest available GIM client module on GIM Server is released for version 12.2.2 (just uploaded <code>12.2.2.0_r123489_1</code>). That is why the Upgrade action is displayed on view. Press Next and then Install button because we do not need modify any parameters for GIM client this time and we can start upgrade immediately.  </p> </li> <li> <p>Press OK button to send upgrade request. </p> </li> <li> <p>Select Show Status link to monitor upgrade progress. After a while all GIM Bundle modules should be successfully updated. </p> </li> <li> <p>Go to GIM Processes Monitor view again and check that new release of GIM client is running on raptor </p> </li> </ol>"},{"location":"v14/gim/#module-configuration","title":"Module configuration","text":"<ol> <li> <p>Go to Set up by Client in cm GUI and select your raptor GIM client again and press Next. </p> </li> <li> <p>Notice that GIM client is updated to the latest release on GIM server and parameter updates are possible only. Press Next button. </p> </li> <li> <p>Select from parameters list GIM_DEBUG and change value to 1. Then push request using Install and OK buttons. </p> </li> <li> <p>In Status pop-up window notice that only some modules requires update. </p> </li> <li> <p>Refresh view, to get confirmation that changes have been successfully deployed. </p> </li> <li> <p>Login to raptor and list installed GIM client modules using <code>configurator.sh</code> tool <pre><code>/opt/guardium/modules/UTILS/current/files/bin/configurator.sh --list\n</code></pre> </p> </li> <li> <p>Check that GIM_DEBUG parameter in GIM module is set to 1. <pre><code>/opt/guardium/modules/UTILS/current/files/bin/configurator.sh --get GIM\n</code></pre> </p> </li> <li> <p>Change GIM_DEBUG value to 0 and confirm change (this time from command line instead from GIM server). <pre><code>/opt/guardium/modules/UTILS/current/files/bin/configurator.sh --set GIM_DEBUG 0\n\n/opt/guardium/modules/UTILS/current/files/bin/configurator.sh --get GIM\n</code></pre> </p> </li> <li> <p>Uninstall GIM client. <pre><code>/opt/guardium/modules/GIM/current/uninstall.pl\n</code></pre></p> </li> </ol>"},{"location":"v14/gim/#gim-module-management-with-api-optional","title":"GIM module management with API (optional)","text":"<ol> <li> <p>Go to gim installers directory on raptor. <pre><code>cd /opt/guardium_tz_bootcamp_automation/upload/source_files/agents/shell\n</code></pre></p> </li> <li> <p>Install again GIM client version 12.2.1. <pre><code>./guard-bundle-GIM-12.2.1.1_r123268_v12_x_1-rhel-9-linux-x86_64.gim.sh -- --dir /opt/guardium --tapip &lt;raptor_ip&gt; --sqlguardip &lt;cm_ip&gt;\n</code></pre></p> </li> <li> <p>In cm cli session list the installed modules on raptor machine. All <code>grdapi</code> commands are run from cm cli session. <pre><code>grdapi gim_list_client_modules clientIP=&lt;raptor_ip&gt;\n</code></pre> </p> </li> <li> <p>Then list of bundles available on GIM server (windows modules are filtered out because client is linux based). <pre><code>grdapi gim_list_bundles\n</code></pre> </p> </li> <li> <p>Assign to GIM client the update of GIM-BUNDLE to the latest version and check module list.</p> <pre><code>grdapi gim_assign_latest_bundle_or_module_to_client module=BUNDLE-GIM clientIP=&lt;raptor_ip&gt;\n\ngrdapi gim_list_client_modules clientIP=&lt;raptor_ip&gt;\n</code></pre> <p>Notice</p> <p>Notice that GIM client 12.2 version has been set as scheduled but schedule time is not set yet.</p> <p></p> </li> <li> <p>Set update schedule. <pre><code>grdapi gim_schedule_install date=now clientIP=&lt;raptor_ip&gt;\n</code></pre></p> </li> <li> <p>Monitor the upgrade process.     <pre><code>grdapi gim_list_client_modules clientIP=&lt;raptor_ip&gt;\n</code></pre></p> <p></p> <p>Note</p> <p>After few minutes the command will provide the output change. It should confirm that GIM client has been updated to the latest version available on GIM server.</p> </li> <li> <p>Now set GIM_DEBUG to 1. <pre><code>grdapi gim_update_client_params paramName=GIM_DEBUG paramValue=1 clientIP=&lt;raptor_ip&gt;\n</code></pre></p> </li> <li> <p>You should notice that change is notified but is not scheduled. <pre><code>grdapi gim_list_client_modules clientIP=&lt;raptor_ip&gt;\n</code></pre> </p> </li> <li> <p>Schedule update. <pre><code>grdapi gim_schedule_install date=now clientIP=&lt;raptor_ip&gt;\n</code></pre></p> </li> <li> <p>Then monitor GIM client state to confirm that schedule is started and finally applied. <pre><code>grdapi gim_list_client_modules clientIP=&lt;raptor_ip&gt;\n</code></pre> </p> </li> <li> <p>Finally uninstall GIM client from raptor. Monitor list of modules on raptor and confirm uninstallation process. <pre><code>grdapi gim_uninstall_module module=BUNDLE-GIM clientIP=&lt;raptor_ip&gt;\n\ngrdapi gim_schedule_uninstall date=now clientIP=&lt;raptor_ip&gt;\n\ngrdapi gim_list_client_modules clientIP=&lt;raptor_ip&gt; \n</code></pre> </p> </li> <li> <p>After a while list of modules assigned to GIM client should be empty. <pre><code>grdapi gim_list_client_modules clientIP=&lt;raptor_ip&gt;\n</code></pre> </p> </li> <li> <p>The list of GIM clients registered to GIM server on cm should be empty as well <pre><code>grdapi gim_list_registered_clients\n</code></pre></p> </li> <li> <p>Because of GIM client issue during uninstallation from API, execute these commands on raptor to clean system <pre><code>systemctl disable guard_gim\n\nrm -f /etc/systemd/system/guard_gim.service\n\nrm -rf /usr/local/guardium\n\nrm -rf /var/log/guard\n</code></pre></p> </li> </ol>"},{"location":"v14/gim/#appendix","title":"Appendix","text":"<p>Dependencies:</p> <p>It is crucial to have modules uploaded on cm for the other agent related labs. So ensure that you finished all non optional taska from this lab.</p> <p>Resources:</p> <ol> <li> <p>List all GIM API functions   https://www.ibm.com/docs/en/guardium/12.x?topic=commands-guardium-installation-manager-gim-apis</p> </li> <li> <p>Certificate management https://www.ibm.com/docs/en/guardium/12.x?topic=management-creating-managing-custom-gim-certificates</p> </li> </ol> <p>Instructor notes:</p>"},{"location":"v14/ktap/","title":"KTAP Deployment","text":""},{"location":"v14/ktap/#kernel-downgrade","title":"Kernel downgrade","text":"<ol> <li> <p>In this lab, the goal is to handle various issues related to KTAP installation. To achieve this, you will install several different kernels on the raptor machine to analyze these scenarios in detail.</p> </li> <li> <p>Check the current raptor kernel level using command. Your default kernel can differ from presented on screenshot. <pre><code>uname -r\n</code></pre> </p> </li> <li> <p>Now we will downgrade kernel to version 5.14.0-570.16.1.el9_6.x86_64. <pre><code>dnf install kernel-5.14.0-570.16.1.el9_6.x86_64 -y\n</code></pre></p> </li> <li> <p>Check the list of available bootable kernels. The newly installed kernel is set as index 0 in the grubby configuration. <pre><code>grubby --info=ALL\n</code></pre> </p> </li> <li> <p>Let\u2019s also confirm that the installed kernel is now set as the default and reboot system. <pre><code>grubby --default-kernel\n\nshutdown -r now\n</code></pre> </p> </li> <li> <p>Sign back in to raptor and confirm that the kernel has been downgraded to version 5.14.0-570.16.1. <pre><code>uname -r\n</code></pre> </p> </li> </ol>"},{"location":"v14/ktap/#stap-installation","title":"STAP installation","text":"<ol> <li> <p>Install GIM client 12.2.2 on raptor. <pre><code>cd /opt/guardium_tz_bootcamp_automation/upload/source_files/agents/shell/\n\n./guard-bundle-GIM-12.2.2.0_r123489_v12_x_1-rhel-9-linux-x86_64.gim.sh -- --dir /opt/guardium --tapip &lt;raptor_ip&gt; --sqlguardip &lt;cm_ip&gt;\n</code></pre></p> </li> <li> <p>To install STAP open Setup by Client view in cm UI, select raptor GIM client and push Next button. Uncheck option Show only latest versions and select from available bundles the BUNDLE-STAP 12.0.6.0_r120251_1 one and push Next. Then in the Choose parameters section set the values for the two parameters and confirm by clicking Next.</p> <p>Insert:</p> <ul> <li>STAP_SQLGUARD_IP: &lt;coll1_ip_address&gt;</li> <li>KTAP_ENABLED: 1</li> </ul> <p>then press Install button and finally request deployment using OK button in Configure clients section and track progress of installation by opening Show Status pop-up window</p> <p></p> </li> <li> <p>Refresh the view till all components installation status will be changed to INSTALLED and then Close pop-up\u2019s </p> </li> <li> <p>In the coll1 UI check the STAP agent status. Open S-TAP control view and expand using  icon the agent menu and select Event log. The pop-up S-TAP Events report shows that kernel module for currently installed on raptor is available an installed. </p> </li> <li> <p>To confirm KTAP installation on the collector the S-TAP Status view and notice that KTAP module is installed. </p> </li> <li> <p>Execute on raptor the commands to confirm that KTAP module is loaded into kernel. <pre><code>lsmod | grep ktap\n\nmodinfo ktap\n</code></pre> </p> </li> <li> <p>Get the details from <code>/var/log/ktap_install.log</code>. Log clearly indicates that the installed agent version includes a module compiled specifically for the current kernel version (Exact match).  <pre><code>cat /var/log/ktap_install.log\n</code></pre> </p> </li> <li> <p>Generate some database activity on raptor and verify whether it is captured by the agent. Switch the session context to the postgres user and use the native PostgreSQL client to execute a dummy query. <pre><code>su - postgres\n\npsql\n</code></pre> <pre><code>SELECT 'activity with kernel 5.14.0-570.16.1.el9_6 and STAP 12.0.6.0_r120251_1';\n\n\\q\n\nexit\n</code></pre></p> </li> <li> <p>In the coll1 UI, open the Training dashboard and verify whether the recently executed query is visible in the Full SQL (Training) report. </p> </li> </ol>"},{"location":"v14/ktap/#kernel-upgrade-stap-revisions","title":"Kernel upgrade, STAP revisions","text":"<ol> <li> <p>Now upgrade the kernel on raptor from 5.14.0-570.16.1.el9_6 to 5.14.0-570.24.1.el9_6 and observe what happens. <pre><code>dnf install kernel-5.14.0-570.24.1.el9_6.x86_64 -y\n\nshutdown -r now\n</code></pre></p> </li> <li> <p>After reboot, confirm that the system is running kernel 5.14.0-570.24.1.el9_6.x86_64. <pre><code>uname -r\n</code></pre></p> </li> <li> <p>Verify that KTAP is not loaded. <pre><code>modinfo ktap\n</code></pre> </p> </li> <li> <p>A file named <code>ktap_install_fail.txt</code> has appeared in the <code>/tmp</code> directory. Review its contents, as it indicates a kernel support issue with the currently running system version. <pre><code>cat /tmp/ktap_install_fail.txt\n</code></pre> </p> </li> <li> <p>The logs indicate that STAP failed to compile the module and did not attempt to locate a flex module. Since no prebuilt module was available, the installation failed. <pre><code>cat /var/log/ktap_install.log\n</code></pre> </p> </li> <li> <p>You can also check S-TAP Events and S-TAP Status in the coll1 UI to clearly confirm that the module-related issue is reported there.  </p> </li> <li> <p>It is also worth checking the modules archive in the KTAP installation directory. A simple search of its contents confirms that no module is available for the current kernel version. <pre><code>cd /opt/guardium/modules/KTAP/current\n\ntar tvf modules-12.0.6.0_r120251_v12_0_1.tgz | grep 5.14.0-570.24.1.el9_6.x86_64\n</code></pre></p> </li> <li> <p>To verify whether your kernel is supported, visit https://ibm.github.io/guardium-ktap/. It turns out that kernel 5.14.0-570.24.1.el9_6 is supported by EXACT precompiled modules for agent 12.0. Support became available with later revision. There is no information about FLEX support \u2639 </p> </li> <li> <p>Let's install the fresher revision of agent 12.0.6. In the cm UI, go again to Setup by Client. After selecting raptor, you will notice that two revisions of the 12.0.6.0_r120251 agent are available in the modules list\u2014revision 1 (currently installed) and revision 35. </p> </li> <li> <p>Confirm that the task has been successfully completed. </p> </li> <li> <p>As expected, the agent revision included a precompiled module, which was successfully loaded. <pre><code>lsmod | grep ktap\n\nmodinfo ktap\n</code></pre> </p> </li> <li> <p>Also confirm that the new modules archive file contains the required module. <pre><code>cd /opt/guardium/modules/KTAP/current\n\ntar tvf modules-12.0.6.0_r120251_v12_0_35.tgz | grep 5.14.0-570.24.1.el9_6.x86_64\n</code></pre> </p> </li> <li> <p>Generate some database activity on raptor and verify whether it is captured by the agent. Switch the session context to the postgres user and use the native PostgreSQL client to execute a dummy query. Check Full SQL (Training) report to confirm that traffic is visible. <pre><code>su - postgres\n\npsql\n</code></pre> <pre><code>SELECT 'activity with kernel 5.14.0-570.24.1.el9_6 and STAP 12.0.6.0_r120251_35';\n\n\\q\n\nexit\n</code></pre> </p> </li> </ol>"},{"location":"v14/ktap/#ktap-with-flex-module","title":"KTAP with flex module","text":"<ol> <li> <p>This time, you will switch to kernel 5.14.0-570.62.1.el9_6.x86_64, but before doing so, verify on https://ibm.github.io/guardium-ktap/ the support for this version for 12.0 agent. It turns out there is no precompiled module available for this kernel version, but you can use the Flex (EXACT-LIKE) mechanism, which allows loading a module built for a kernel version close to the target one.  </p> </li> <li> <p>You can also verify exactly which compiled modules can be used as Flex (EXACT LIKE) for the target kernel. The file <code>ktap-combos.txt</code> contains their list. In this case, the currently installed revision 35 includes three modules that can be used with kernel 5.14.0-570.62.1.el9_6. <pre><code>cd /opt/guardium/modules/KTAP/current\n\ncat ktap-combos.txt | grep 5.14.0-570.62.1.el9_6\n</code></pre> </p> </li> <li> <p>Before upgrading the kernel, enable support for Flex modules. In the cm UI, go to Setup by Client, and for revision 35, add the additional parameter KTAP_ALLOW_MODULE_COMBOS with the value Y.  </p> </li> <li> <p>Now upgrade the kernel and reboot the system. <pre><code>dnf install kernel-5.14.0-570.62.1.el9_6.x86_64 -y\n\nshutdown -r now\n</code></pre></p> </li> <li> <p>Check again whether the KTAP module is available and notice that it is still using the module built for version 5.14.0-570.24.1, which is compatible with kernel 5.14.0-570.62.1. <pre><code>lsmod | grep ktap\n\nmodinfo ktap\n</code></pre> </p> </li> <li> <p>Additionally check <code>/var/log/ktap_install.log</code> and notice that module compiled for kernel 5.14.0-570.24.1.el9_6 has been selected as flexible to cover currently installed. <pre><code>cat /var/log/ktap_install.log\n</code></pre> </p> </li> <li> <p>Generate some database activity on raptor and verify whether it is captured by the agent. Switch the session context to the postgres user and use the native PostgreSQL client to execute a dummy query. <pre><code>su - postgres\n\npsql\n</code></pre> <pre><code>SELECT 'activity with FLEX module on kernel 5.14.0-570.62.1.el9_6 and STAP 12.0.6.0_r120251_35';\n\n\\q\n\nexit\n</code></pre> </p> </li> </ol>"},{"location":"v14/ktap/#uninstall-stap","title":"Uninstall STAP","text":"<ol> <li> <p>Now simulate a common troubleshooting mistake\u2014attempting to reinstall the agent while the KTAP module is still loaded. First, uninstall the STAP agent. In Setup by Client, select the currently installed STAP version (12.0.6.0_r120251_35) and click Uninstall. Confirm that the agent has been successfully removed.  </p> </li> <li> <p>Refresh Status pop-up till message that BUNDLE-STAP is NOT_INSTALLED will appear.  </p> </li> <li> <p>Try again install BUNDLE-STAP 12.0.6.0_r120251_35 again.</p> <p>Set parameters:</p> <ul> <li>KTAP_ALLOW_MODULE_COMBOS: Y</li> <li>KTAP_ENABLED: 1</li> <li>STAP_SQLGUARD_IP: &lt;coll1.demo.guardium&gt;</li> </ul> <p> </p> </li> <li> <p>This time the installation will fail because previously installed kernel module was not unloaded  </p> </li> <li> <p>Uninstallation process does not include the kernel module unload (what is very dangerous command on heavily used production systems). That is why the module will disappear only after system reboot. Because of that, any try to install STAP again will fail because of old KTAP appearance in kernel space. Check <code>ktap_install.log</code> to on raptor to confirm this situation. <pre><code>cat /var/log/ktap_install.log\n</code></pre> </p> </li> <li> <p>Also <code>lsmod</code> command provides information that KTAP module from previous installation is still loaded and <code>modinfo</code> will point that module file has been removed from filesystem <pre><code>lsmod | grep ktap\n\nmodinfo ktap\n</code></pre> </p> </li> <li> <p>Restart raptor machine to clean up kernel space <pre><code>shutdown -r now\n</code></pre></p> </li> </ol>"},{"location":"v14/ktap/#stap-upgrade","title":"STAP upgrade","text":"<ol> <li> <p>Install again BUNDLE-STAP 12.0.6.0_r120251_35.</p> <p>Set parameters:</p> <ul> <li>KTAP_ALLOW_MODULE_COMBOS: Y</li> <li>KTAP_ENABLED: 1</li> <li>STAP_SQLGUARD_IP: &lt;coll1.demo.guardium&gt;</li> </ul> <p> </p> </li> <li> <p>This time, the installation succeeded because the raptor system was rebooted and the previously loaded module was unloaded.  <pre><code>lsmod | grep ktap\n</code></pre> </p> </li> <li> <p>To upgrade STAP from 12.0.6 to 12.2.2.0 select in cm UI in the Set up by Client select raptor GIM client and choose BUNDLE-STAP 12.2.2.0_r123489_3.  </p> </li> <li> <p>Upgrade STAP by pressing Install button and confirm that it was successful.  </p> </li> <li> <p>Check KTAP modules loaded on raptor machine and noticed that we have two of them for builds 120251 (12.0.6) and 123489 (12.2.2.0). The older one will be unloaded after next system reboot <pre><code>lsmod | grep ktap\n</code></pre> </p> </li> <li> <p>Confirm that 12.2.2 has precompiled module for 5.14.0-570.62.1.el9_6. <pre><code>modinfo ktap\n</code></pre> </p> </li> <li> <p>Generate some database activity on raptor and verify whether it is captured by the agent. Switch the session context to the postgres user and use the native PostgreSQL client to execute a dummy query. <pre><code>su - postgres\n\npsql\n</code></pre> <pre><code>SELECT 'activity with EXACT module on kernel 5.14.0-570.62.1.el9_6 and STAP 12.2.2.0_r123489_3 after upgrade';\n\n\\q\n\nexit\n</code></pre> </p> </li> <li> <p>Restart raptor machine. <pre><code>shutdown -r now\n</code></pre></p> </li> <li> <p>And then check that only the latest module (12.2.2.0) is now loaded only into kernel. <pre><code>lsmod|grep ktap\n</code></pre> </p> </li> <li> <p>To disable Flex module support we must uninstall STAP agent because flag STAP_ALLOW_MODULE_COMBO cannot be switched off if is set to Y before. Reboot raptor system again. <pre><code>/opt/guardium/modules/GIM/current/uninstall.pl\n\nshutdown -r now\n</code></pre></p> </li> </ol>"},{"location":"v14/ktap/#self-compiled-kernel-module","title":"Self compiled kernel module","text":"<ol> <li> <p>Install GIM client again on raptor. <pre><code>cd /opt/guardium_tz_bootcamp_automation/upload/source_files/agents/shell/\n\n./guard-bundle-GIM-12.2.2.0_r123489_v12_x_1-rhel-9-linux-x86_64.gim.sh -- --dir /opt/guardium --tapip &lt;raptor_ip&gt; --sqlguardip &lt;cm_ip&gt;\n</code></pre></p> </li> <li> <p>Then install STAP 12.2.2.0_r123489_3 without Flex module support.</p> <p>Set parameters:</p> <ul> <li>KTAP_ALLOW_MODULE_COMBOS: N</li> <li>KTAP_ENABLED: 1</li> <li>STAP_SQLGUARD_IP: &lt;coll1.demo.guardium&gt;</li> <li>KTAP_PREVENT_EXACT_MATCH_BUILD: N</li> </ul> <p>Also note that variable KTAP_PREVENT_EXACT_MATCH_BUILD is set to N as a default, which means the system will attempt to compile the module if it is not available in the distribution. This variable cannot be changed after the agent is installed. If you want the agent to skip compilation attempts, it must be set to Y during the agent installation.</p> <p></p> </li> <li> <p>Check the version of initially installed kernel on raptor machine. Your kernel version may be newer than the one shown in the screenshot, as the cloud environment is automatically updated with important kernel releases.  <pre><code>grubby --info=ALL\n</code></pre> </p> </li> <li> <p>Regardless of which kernel version is currently the latest, it is not supported by the currently installed agent 12.2.2.0_r123489_3 using precompiled module.</p> </li> <li> <p>Set the latest available kernel as active and reboot the system. <pre><code>grubby --set-default-index=2\n\ngrubby --default-kernel\n\nshutdown -r now\n</code></pre> </p> </li> <li> <p>After the reboot, it is not surprising that KTAP was not installed. <pre><code>uname -r\n\nmodinfo ktap\n</code></pre> </p> </li> <li> <p>The <code>/var/log/ktap_install.log</code> clearly shows that the Flex module was not loaded because the feature was disabled, and no compilation attempt was made since the required kernel development environment is missing. <pre><code>cat /var/log/ktap_install.log\n</code></pre> </p> </li> <li> <p>To make kernel module compilable install <code>kernel-devel</code> for current kernel version <pre><code>dnf install -y kernel-devel-$(uname -r)\n</code></pre></p> </li> <li> <p>Go back to the agent configuration on raptor, set KTAP_ENABLED from 0 to 1, and confirm the change. </p> </li> <li> <p>Check <code>/var/log/ktap_install.log</code> again and notice that kernel module has been compiled this time. <pre><code>cat /var/log/ktap_install.log\n</code></pre> </p> </li> <li> <p>Check if the module has been loaded <pre><code>modinfo ktap\n</code></pre> </p> </li> <li> <p>Generate some database activity on raptor and verify whether it is captured by the agent. Switch the session context to the postgres user and use the native PostgreSQL client to execute a dummy query. <pre><code>su - postgres\n\npsql\n</code></pre> <pre><code>SELECT 'activity with CUSTOM module on kernel 5.14.0-570.114.1.el9_6 and STAP 12.2.2.0_r123489_3';\n\n\\q\n\nexit\n</code></pre> </p> </li> </ol>"},{"location":"v14/ktap/#practical-custom-modules-usage","title":"Practical custom modules usage","text":"<ol> <li> <p>Compiling modules appears to be the most effective approach for managing agents in environments with frequent Linux kernel updates. However, the main drawback of this approach is the requirement to have development tools available on monitored/production systems, which in most cases violates security policies.</p> <p>In practice, kernel modules are therefore compiled in advance in a local development/test environment and then transferred to production, where they are loaded at the time of a kernel change.</p> </li> <li> <p>Verify that the module compiled in the previous section is physically present in <code>/opt/guardium/modules/KTAP/current</code> and that its filename contains the xCUSTOMX label as well as the kernel version for which it was compiled. <pre><code>cd /opt/guardium/modules/KTAP/current\n\nls\n</code></pre> </p> </li> <li> <p>There are two methods to manage this process: manual and using a GIM server. Let\u2019s start by reviewing the manual process. The first method provides the possibility to add just compiled kernel module in <code>/opt/guardium/modules/KTAP/current</code> to modules archive (<code>modules-&lt;agent_build_number&gt;.tgz</code>) and manually copy it on target machine. <pre><code>./guard_ktap_append_modules\n\ntar tvf modules-12.2.2.0_r123489_v12_x_3.tgz | grep 5.14.0-570.114.1\n</code></pre> </p> </li> <li> <p>Now simply copy the modules archive to a machine with the same agent version. After upgrading the kernel and rebooting, the module should be loaded automatically.</p> <p>However, remember that the decision to allow loading custom (externally provided) modules must be made during the GIM agent installation, which will be covered next.</p> </li> <li> <p>The second method involves installing a new agent revision using GIM, which should automatically appear on the GIM server after module compilation. This is the default behavior and can be controlled using the STAP_UPLOAD_FEATURE variable on source module agent.     In the cm UI, open the Upload Module view. At the end of the list, a new entry should appear with revision 800 for STAP 12.2.2.0_r123489. This is the version uploaded by raptor after compiling the custom module in the previous section.     </p> </li> <li> <p>However, to use this module, the GIM client must be installed with an additional option enabled. To simulate installing a self-compiled module on a production machine, first completely uninstall STAP and GIM, and then reboot the raptor machine. <pre><code>cd\n\n/opt/guardium/modules/GIM/current/uninstall.pl\n\nshutdown -r now\n</code></pre></p> </li> <li> <p>Login to the raptor again. We must uninstall <code>kernel-devel</code> package to avoid module compilation during installation. <pre><code>dnf -y remove kernel-devel-$(uname -r)\n</code></pre></p> </li> <li> <p>Reinstall the GIM client with the additional option <code>install_customed_bundles</code>, which allows STAP to install custom modules provided from a lab/test environment. <pre><code>cd /opt/guardium_tz_bootcamp_automation/upload/source_files/agents/shell/\n\n./guard-bundle-GIM-12.2.2.0_r123489_v12_x_1-rhel-9-linux-x86_64.gim.sh -- --dir /opt/guardium --install_customed_bundles --tapip &lt;raptor_ip&gt; --sqlguardip &lt;cm_ip&gt;\n</code></pre></p> </li> <li> <p>Then, install STAP on raptor using an agent with the compiled and packaged CUSTOM KTAP before - 12.2.2.0_r123489_800 (wait for raptor gim client appearance). You must unselect Show only latest version option and choose Include custom bundles to see the custom one. Ensure that you disabled the Flex option. You will also need to confirm that you want to install the custom module. Finally, verify that the installation completed successfully.</p> <p>Set parameters:</p> <ul> <li>KTAP_ALLOW_MODULE_COMBOS: N</li> <li>KTAP_ENABLED: 1</li> <li>STAP_SQLGUARD_IP: &lt;coll1.demo.guardium&gt;</li> </ul> <p> </p> </li> <li> <p>Confirm that custom compiled and distributed by GIM kernel module was used <pre><code>cat /var/log/ktap_install.log\n\nmodinfo ktap\n</code></pre> </p> </li> <li> <p>To prepare the environment for the next lab, uninstall STAP and GIM on the raptor machine. <pre><code>cd\n\n/opt/guardium/modules/GIM/current/uninstall.pl\n\nshutdown -r now\n</code></pre></p> </li> </ol>"},{"location":"v14/ktap/#appendix","title":"Appendix","text":"<p>Dependencies:</p> <p>This chapter can be omitted if you are not interested in KTAP management on Linux machines. You can jump to ATAP lab.</p> <p>Resources:</p> <ol> <li>Kernel modules locator   https://ibm.github.io/guardium-ktap/index.html</li> </ol> <p>Instructor notes:</p>"},{"location":"v14/logstash/","title":"Universal connector 1.0 - logstash based","text":""},{"location":"v14/logstash/#cassandra-configuration-check","title":"Cassandra configuration check","text":"<ol> <li> <p>On sauropod machine as a root confirm that Cassandra cluster is up. <pre><code>nodetool status\n</code></pre> </p> </li> <li> <p>Confirm that <code>audit.log</code> file appeared in <code>/var/log/cassandra/audit/</code>. <pre><code>ls /var/log/cassandra/audit/audit.log\n</code></pre> </p> </li> <li> <p>Login to Cassandra on sauropod and generate some events. <pre><code>cqlsh\n\nCREATE KEYSPACE movies WITH REPLICATION = { 'class' : 'SimpleStrategy',     'replication_factor' : 1 };\n\nuse movies;\n\nCREATE TABLE movie (id UUID PRIMARY KEY, m_title text, m_release int);\n\nINSERT INTO movie (id, m_title, m_release) VALUES (uuid(), 'Alien', 1979);\n\nselect * from movie;\n\nexit\n</code></pre></p> </li> <li> <p>Check audit log to confirm that events are stored in. <pre><code>cat /var/log/cassandra/audit/audit.log\n</code></pre> </p> </li> </ol>"},{"location":"v14/logstash/#filebeat-installation-and-setup","title":"Filebeat installation and setup","text":"<ol> <li> <p>Check the latest version of <code>filebeat</code> on Elastic web page - https://www.elastic.co/downloads/beats/filebeat </p> </li> <li> <p>Download the <code>filebeat</code> rpm (replace filebeat_version variable to the latest one) - on sauropod machine: <pre><code>mkdir -p /root/lab_files\n\ncd /root/lab_files\n\ncurl -L -O https://artifacts.elastic.co/downloads/beats/filebeat/filebeat-&lt;filebeat_version&gt;-x86_64.rpm\n</code></pre></p> </li> <li> <p>Install <code>filebeat</code>. <pre><code>dnf -y install /root/lab_files/filebeat-&lt;filebeat_version&gt;-x86_64.rpm\n</code></pre></p> </li> <li> <p>Edit <code>/etc/filebeat/filebeat.yml</code>. Modify filebeat.inputs section to form below (use spaces in the indendation, it is YAML file, format is indentation aware!): <pre><code>- type: filestream\n  id: \"cassandra\"\n  enabled: true\n  paths:\n      - /var/log/cassandra/audit/audit.log\n  exclude_lines: ['AuditLogManager']\n  tags: [\"cassandra\"]\n  multiline.type: pattern\n  multiline.pattern: '^INFO'\n  multiline.negate: true\n  multiline.match: after\n</code></pre> Comment or remove the entire section output.elasticsearch Modify output.logstash section to form below, it is important to use coll1.demo.guardium name because we will later set the encrypted connection between <code>filebeat</code> and coll1. Do not forget to uncomment output.logstash section row as well. <pre><code># ------------------------------ Logstash Output ---------------------------\noutput.logstash:\n  # The Logstash hosts\n  hosts: [\"coll1.gdemo.com:5047\"]\n</code></pre> </p> </li> <li> <p>Start filebat on sauropod. <pre><code>systemctl start filebeat\n\nsystemctl status filebeat\n\nsystemctl enable filebeat\n</code></pre></p> </li> </ol>"},{"location":"v14/logstash/#install-and-configure-uc-plugin","title":"Install and configure UC plugin","text":"<ol> <li> <p>Starting from Guardium 12.1 the new architecture of universal connectors with Kafka Connect consumer was introduced. In this case the UC can be managed from Central Manager only. Still the legacy configuration workflow with logstash can be enabled and it will be used in this lab. To enable old configuration approach we must enable legacy workflow by executing this command on coll1 (from cli session) <pre><code>grdapi modify_guard_param paramName=LEGACY_UC_CONFIG_ENABLED paramValue=1\n</code></pre></p> </li> <li> <p>Run universal connector engine on coll1. <pre><code>grdapi run_universal_connector\n</code></pre></p> </li> <li> <p>There is only one Cassandra filter plugin version \u2013 1.0.1 and it is preinstalled on coll1. You can check a list of all actually available universal connector plugins on appliance by execution command below on coll1: <pre><code>grdapi show_universal_connector_plugins\n</code></pre> One on the list is <code>cassandra_guardium_plugin_filter</code> \u2013 revision 1.0.1. If new universal connector plugin version will be released and it is not preloaded on your collector you should upload it on appliance (it is not our case).</p> </li> <li> <p>Login to coll1 UI, confirm that there is no universal connectors configured (Configure Universal Connector view), the universal connector engine is running and add new instance using icon . </p> </li> <li> <p>Configure universal connector instance for Cassandra with Connector name set to cassandra.  In the Input configuration section modify port used in filebeat configuration (select appropriate port). Use spaces for indentation: <pre><code>beats {\n  port =&gt; \"5047\"\n  type =&gt; \"filebeat\"\n}\n</code></pre>  In the Filter configuration section replace content by configuration below: <pre><code>if [type] == \"filebeat\" {\n  mutate { add_field =&gt; { \"serverIP\" =&gt; \"%{[@metadata][ip_address]}\" } add_field   =&gt; { \"serverHostname\" =&gt; \"%{[host][name]}\" } }\n  cassandra_guardium_plugin_filter{}\n  mutate { remove_field =&gt; [\"serverHostname\",\"@version\",\"@timestamp\",\"type\",\"sequence\",\"message\",\"host\",\"tags\",\"input\",\"log\",\"ecs\",\"agent\",\"serverIP\"]}\n}\n</code></pre> </p> </li> <li> <p>Save configuration and wait a while for success confirmation (takes a while). </p> </li> <li> <p>Confirm that new instance is displayed on the list </p> </li> <li> <p>Open S-TAP Status view. You should see a new Cassandra universal connector instance listed. If the status will be Inactive it means that there is no currently traffic processes but our configuration but universal connector is set correctly. </p> </li> </ol>"},{"location":"v14/logstash/#check-traffic-visibility","title":"Check traffic visibility","text":"<ol> <li> <p>Execute some activity in Cassandra instance on sauropod. <pre><code>cqlsh\n\nuse movies;\n\nINSERT INTO movie (id, m_title, m_release) VALUES (uuid(), 'Aliens', 1986);\n\nINSERT INTO movie (id, m_title, m_release) VALUES (uuid(), 'Alien 3', 1992);\n\nINSERT INTO movie (id, m_title, m_release) VALUES (uuid(), 'Alien: Resurrection', 1997);\n\nINSERT INTO movie (id, m_title, m_release) VALUES (uuid(), 'Alien: Romulus', 2024);\n\nINSERT INTO movie (id, m_title, m_release) VALUES (uuid(), 'Alien: Earth', 2025);\n\nselect * from movie;\n\nexit;\n</code></pre> </p> </li> <li> <p>Check Full SQL (Training) report on coll1. </p> </li> <li> <p>In the coll1 UI open S-TAP Status view. Just installed universal connector instance should be now Active.  </p> </li> </ol>"},{"location":"v14/logstash/#set-tls-connection-between-filebeat-and-logstash-optional","title":"Set TLS connection between filebeat and logstash (optional)","text":"<ol> <li> <p>On coll1 regenerate self-signed certificate for universal connector and copy public certificate to buffer (domain is demo.guardium) <pre><code>grdapi generate_ssl_key_universal_connector overwrite=1 hostname=*.demo.guardium\n</code></pre> </p> </li> <li> <p>Save just created above certificate into the file <code>/etc/filebeat/guardium.pem</code> on sauropod machine. </p> </li> <li> <p>Check logstash TLS support on the coll1 by checking the server port and notice that TLS is not available. <pre><code>openssl s_client -connect coll1.demo.guardium:5047\n</code></pre> </p> </li> <li> <p>Login to coll1 UI and edit universal connector instance definition (Configure Universal Connector view). Add some lines related to use encrypted communication between filebeat and logstash in the Input configuration section (remember about correct indentation): <pre><code>  ssl =&gt; true\n  ssl_certificate =&gt; \"${SSL_DIR}/cert.pem\"\n  ssl_key =&gt; \"${SSL_DIR}/key.pem\"\n</code></pre> </p> </li> <li> <p>Check TLS logstash readiness on coll1 from sauropod machine <pre><code>openssl s_client -connect coll1.demo.guardium:5047 &lt; /dev/null\n</code></pre> </p> </li> <li> <p>Modify output.logstash section in <code>/etc/filebeat/filebeat.yml</code> store on sauropod machine by enabling ssl.certificate_authorities parameter with path to file created in point 2 (<code>/etc/filebeat/guardium.pem</code>)  </p> </li> <li> <p>Restart filebeat service on sauropod machine <pre><code>systemctl restart filebeat\n\nsystemctl status filebeat\n</code></pre></p> </li> <li> <p>Execute again some SQL\u2019s <pre><code>cqlsh\n\nuse movies;\n\nSELECT * FROM movie;\n\nselect * from movie where m_release &gt; 2000 ALLOW FILTERING;\n\nexit;\n</code></pre> </p> </li> </ol>"},{"location":"v14/logstash/#appendix","title":"Appendix","text":"<p>Dependencies:</p> <p>If your goal is not related to practicing universal connectors based on logstash, you may skip this lab, with the understanding that some of the other labs may refer to a non existent instance of the Cassandra universal connector in your environment. I suggest do not deploy logstash and later kafka based traffic to coll1, so do not join these two labs one after another.</p> <p>Resources:</p> <ul> <li>Universal Connector documentation https://www.ibm.com/docs/en/guardium/12.x?topic=guardium-universal-connector</li> <li>Filebeat logstash output documentation https://www.elastic.co/guide/en/beats/filebeat/current/logstash-output.html</li> <li>Available UC plugins https://github.com/IBM/universal-connectors/blob/main/docs/available_plugins.md</li> <li>Cassandra audit logging configuration https://cassandra.apache.org/doc/stable/cassandra/operating/audit_logging.html#view-the-contents-of-auditlog-files</li> </ul> <p>Instructor notes:</p>"}]}